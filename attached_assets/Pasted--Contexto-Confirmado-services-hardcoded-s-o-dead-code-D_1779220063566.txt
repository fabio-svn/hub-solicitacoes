## Contexto

Confirmado: services hardcoded são dead code. Deletar.
Plus: 2 fixes secundários relacionados.

---

## Fix R5.0 — Deletar services hardcoded

DELETAR completamente:
- artifacts/api-server/src/services/assinatura-generator.ts
- artifacts/api-server/src/services/welcome-card-generator.ts

Não há imports — confirmado via grep. Se aparecer erro de build, 
algum import escondido foi perdido — reverter e me avisar.

---

## Fix R5.1 — Sincronizar DB_STATEMENTS com schema

Em artifacts/api-server/src/index.ts:

ADICIONAR após linha 104 (após o ALTER TABLE de created_at):

  `ALTER TABLE "art_templates" ADD COLUMN IF NOT EXISTS "variant_value" VARCHAR(100)`,

ADICIONAR antes de "Art assets" (linha 106):

  // User-Tipo assignments
  `CREATE TABLE IF NOT EXISTS "user_tipo_assignments" (
    "id"      SERIAL       PRIMARY KEY,
    "user_id" INTEGER      NOT NULL REFERENCES "users" ("id") ON DELETE CASCADE,
    "tipo"    VARCHAR(50)  NOT NULL,
    UNIQUE ("user_id", "tipo")
  )`,
  `CREATE INDEX IF NOT EXISTS "IDX_user_tipo_assignments_user_id" ON "user_tipo_assignments" ("user_id")`,

---

## Fix R5.2 — UI de erro no thankyou.html

Em artifacts/api-server/public/thankyou.html, função 
iniciarPollingArtefato (~linha 84):

LOCALIZAR o bloco `.then(data => { ... })` (linha 94).

ADICIONAR no início do bloco, antes de `if (data.ready && ...)`:

```js
if (data.status === 'erro') {
  if (_artefatoPollingTimer) clearTimeout(_artefatoPollingTimer);
  const pollingEl = document.getElementById('artefatoPolling');
  if (pollingEl) pollingEl.style.display = 'none';
  const readyEl = document.getElementById('artefatoReady');
  if (readyEl) {
    readyEl.innerHTML = `
      <div style="color:#dc2626;padding:16px;background:rgba(220,38,38,0.08);border-radius:8px;text-align:center">
        <strong>Erro na geração</strong>
        <p style="margin-top:8px;font-size:0.85rem;opacity:0.8">
          Houve um problema ao gerar seu material. 
          Entre em contato com o time de Marketing.
        </p>
      </div>`;
    readyEl.style.display = 'block';
  }
  return;
}
```

(adaptar nome de `_artefatoPollingTimer` se diferente no arquivo)

---

## Critério de validação

1. `pnpm run build` compila sem erros (validar que não houve imports 
   escondidos pros services deletados)
2. Submeter assinatura normal — continua gerando arte e salvando no R2
3. Forçar erro via SQL: `UPDATE solicitacoes SET status='erro' WHERE id=X`
4. Abrir /thankyou.html?id=X — mostra mensagem de erro vermelha em 
   vez de loop infinito
5. Deploy em ambiente novo (greenfield) — banco se cria com 
   variant_value e user_tipo_assignments

## Não faça

- Não toque em admin.ts (ApiError adoption fica pra refactor focado)
- Não toque em outros services (template-renderer, art-generator)
- Não toque em forms.ts, gerador-*.ts