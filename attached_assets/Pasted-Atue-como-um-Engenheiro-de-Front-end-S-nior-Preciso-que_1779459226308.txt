Atue como um Engenheiro de Front-end Sênior. Preciso que você aplique uma correção no arquivo "artifacts/api-server/public/admin-templates.html" para resolver um bug de sobreposição do Live Preview que está impedindo o arrasto de elementos (Drag and Drop) e causando duplicação visual (efeito fantasma) das layers.

Por favor, faça as seguintes alterações exatas no arquivo:

### 1. Correção no CSS (Tag <style>)
- Localize a classe `.canvas-preview-img` e adicione a propriedade `pointer-events: none;` para que a imagem gerada pelo backend não roube os cliques do mouse.
- Logo abaixo de `.canvas-preview-img`, adicione uma nova regra CSS: 
`#canvasWrap.live-preview-active .layer-content-preview { opacity: 0; }`

### 2. Correção na função `renderCanvas()` (Tag <script>)
- Localize onde o `z-index` da div principal da layer é definido. Modifique a linha para adicionar +10 ao z-index, garantindo que as caixas interativas fiquem acima do preview:
De: `div.style.cssText = \`left:\${x}px;top:\${y}px;width:\${w}px;height:\${h < 18 ? 18 : h}px;z-index:\${total - idx};\`;`
Para: `div.style.cssText = \`left:\${x}px;top:\${y}px;width:\${w}px;height:\${h < 18 ? 18 : h}px;z-index:\${total - idx + 10};\`;`
- Ainda dentro do loop de `renderCanvas()`, precisamos aplicar a classe 'layer-content-preview' no invólucro interno de cada tipo de layer.
  - Para a layer de Forma (Shape), onde `svgWrap` é criado, adicione: `svgWrap.className = 'layer-content-preview';`
  - Para a layer de Texto (Text-line / Text-block), onde `txtPreview` é criado, adicione: `txtPreview.className = 'layer-content-preview';`
  - Para a layer de Imagem (Image), onde `imgWrap` é criado, adicione: `imgWrap.className = 'layer-content-preview';`

### 3. Atualização no Controle de Preview (`toggleLive` e `doLivePreview`)
Substitua as funções `toggleLive()` e `doLivePreview()` inteiras por este código atualizado que controla a classe `live-preview-active`:

```javascript
    function toggleLive() {
      liveEnabled = !liveEnabled;
      const btn = document.getElementById('liveToggleBtn');
      btn.classList.toggle('on', liveEnabled);
      btn.textContent = liveEnabled ? '● Live' : '○ Live';
      const manualBtn = document.getElementById('manualPreviewBtn');
      if (manualBtn) manualBtn.style.display = liveEnabled ? 'none' : '';
      
      if (liveEnabled) {
        scheduleLivePreview(0);
      } else {
        document.getElementById('previewImg').style.display = 'none';
        document.getElementById('canvasWrap').classList.remove('live-preview-active');
      }
    }

    async function doLivePreview() {
      if (!currentTemplate) return;
      if (previewAbortCtrl) previewAbortCtrl.abort();
      previewAbortCtrl = new AbortController();
      showRenderingIndicator(true);
      try {
        const res = await fetch('/api/admin/art-templates/preview', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          credentials: 'include',
          body: JSON.stringify({ config: currentTemplate, data: testData }),
          signal: previewAbortCtrl.signal,
        });
        if (!res.ok) return;
        const blob = await res.blob();
        const img = document.getElementById('previewImg');
        const oldUrl = img.src;
        img.src = URL.createObjectURL(blob);
        img.style.display = 'block';
        
        document.getElementById('canvasWrap').classList.add('live-preview-active');
        
        if (oldUrl && oldUrl.startsWith('blob:')) URL.revokeObjectURL(oldUrl);
      } catch (e) {
        if (e.name !== 'AbortError') console.error('[live preview]', e);
      } finally {
        showRenderingIndicator(false);
      }
    }