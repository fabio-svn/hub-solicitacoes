## Contexto

Remoção completa do tipo "certificado-eventos" do Hub SVN. Esse fluxo 
vai migrar pra outro sistema e não é mais necessário aqui. 

Múltiplos arquivos afetados — tarefa de limpeza coordenada.

## Decisão sobre histórico

ANTES de aplicar, verificar se há solicitações antigas no banco:

  SELECT COUNT(*) FROM solicitacoes WHERE tipo_solicitacao = 'certificado-eventos';

Se 0: remover tudo incluindo TIPO_SOLICITACAO_LABELS.
Se > 0: manter APENAS a entrada em TIPO_SOLICITACAO_LABELS no 
  config.js (linha 107) — pro histórico das antigas continuar 
  mostrando "Certificado para Eventos" em vez do slug bruto.

---

## Tarefa 1 — Frontend public/config.js

1. Remover do array TIPO_SOLICITACAO (linha 53):
   { id: "certificado-eventos", label: "Certificado para Eventos", icon: "icon-award", ativo: true },

2. Remover do FORM_PAGES (linha 446):
   "certificado-eventos": "form-certificado-eventos.html",

3. CONDICIONAL: na linha 107 do TIPO_SOLICITACAO_LABELS:
   "certificado-eventos": "Certificado para Eventos",
   - Se SQL acima retornou 0: REMOVER esta linha
   - Se retornou > 0: MANTER

NÃO MEXER na linha 200 (palestrante-svn / palestrante_certificado.webp) — 
esse "certificado" é parte do nome de um asset diferente (palestrante), 
não relacionado ao tipo certificado-eventos.

---

## Tarefa 2 — Deletar form

Deletar o arquivo:
  artifacts/api-server/public/form-certificado-eventos.html

(Esse arquivo tem ~200 linhas. Apenas deleta.)

---

## Tarefa 3 — Admin templates (public/admin-templates.html)

Linha ~598 — remover entrada de TIPO_LABELS:
  'certificado-eventos': 'Certificado para Eventos',

Linha ~614 — remover entrada de createTipoOptions:
  { value: 'certificado-eventos', label: 'Certificado para Eventos' },

---

## Tarefa 4 — Solicitacao.html

Linha ~555 — remover do array de tipos (provavelmente TIPOS_AUTOMACAO 
ou similar):
  'certificado-eventos',

Verificar se a vírgula adjacente fica correta após a remoção.

---

## Tarefa 5 — Thankyou.html

Linha ~72 — remover do TIPOS_AUTOMACAO array:
  'certificado-eventos',

---

## Tarefa 6 — Backend form-schemas.ts

Em artifacts/api-server/src/routes/form-schemas.ts, linhas ~177-188:

Remover o bloco INTEIRO:

  'certificado-eventos': {
    tipo: 'certificado-eventos',
    label: 'Certificado para Eventos',
    fields: [
      { name: 'telefone',       ... },
      { name: 'nome_completo',  ... },
      { name: 'email',          ... },
      { name: 'nome_evento',    ... },
      { name: 'id_evento',      ... },
      { name: 'carga_horaria',  ... },
    ],
  },

---

## Tarefa 7 — Backend routes/forms.ts

1. Linha ~102 — remover do REQUIRED_FIELDS:
   "certificado-eventos": ["nome", "nomeCompleto", "whatsapp", "emailCertificado", "idEvento", "cargaHoraria"],

2. Linha ~199 — remover do switch case:
   case "certificado-eventos":  return `[Certificado] ${s(dados.nomeCompleto) || userName}`;

---

## Tarefa 8 — clickup.ts (se houver)

Buscar:
  grep -n "certificado-eventos\|certificado_eventos" \
    artifacts/api-server/src/routes/clickup.ts

Se houver case em algum switch, REMOVER. Se houver UUID/config 
específico, REMOVER.

---

## Tarefa 9 — Backend forms.ts STATUS_FLOWS / outros

Buscar restantes:
  grep -rn "certificado-eventos\|certificado_eventos" \
    artifacts/api-server/src/

Pra cada ocorrência remanescente fora de migrations ou arquivos 
que já tratamos: avaliar contexto e remover (mas REPORTAR antes de 
deletar se não for óbvio).

NÃO TOQUE em arquivos de migrations existentes ou em dados históricos.

---

## Critério de validação

1. Reload do app:
   pnpm run build  →  sem erros de TypeScript

2. /index.html (ou home) — botão "Certificado para Eventos" sumiu da 
   lista de solicitações.

3. /admin-templates.html — opção "Certificado para Eventos" sumiu do 
   dropdown de criar template.

4. /form-certificado-eventos.html — retorna 404 (arquivo deletado).

5. Se houve solicitações antigas (caso B):
   - /admin.html (ou /solicitacoes.html) ainda mostra as antigas com 
     label "Certificado para Eventos" (não slug bruto)
   - Clicar numa solicitação antiga abre /solicitacao.html?id=X 
     normalmente
   - Resumo carrega os campos antigos sem erro

6. Tentar acessar /form-certificado-eventos.html via URL direta → 404

7. Buscar referências remanescentes:
   grep -rn "certificado-eventos\|certificado_eventos\|emailCertificado\|cargaHoraria" \
     artifacts/api-server/
   
   Esperado: apenas no arquivo de migrations (se existir) e talvez no 
   schema do banco. Nenhuma referência em código ativo.

## Não faça

- Não delete dados do banco. Migrations de remoção da tabela ou 
  colunas relacionadas NÃO são parte deste fix.
- Não toque na linha 200 do config.js (palestrante_certificado.webp 
  é asset de outro tipo).
- Não delete entradas no TIPO_SOLICITACAO_LABELS sem confirmar caso 
  A vs caso B no início.
- Se houver chave em arquivo que NÃO consegue identificar com 
  certeza se é relacionado ao tipo a ser removido, REPORTAR em vez 
  de deletar.