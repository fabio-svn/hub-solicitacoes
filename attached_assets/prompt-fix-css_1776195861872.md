# Correção — style.css

Aplicar os ajustes abaixo no arquivo `style.css`.

---

## 1. Remover duplicação de `.preview-assessor-left`

Localizar as duas definições de `.preview-assessor-left` e substituir por uma única:

```css
/* REMOVER as duas definições existentes e substituir por esta única: */
.preview-assessor-left {
  display: flex;
  flex-direction: column;
  align-items: center;
  text-align: center;
  justify-content: center;
}
```

---

## 2. Corrigir animação duplicada do `.modal-card`

Na regra base do `.modal-card`, remover o `animation`:

```css
/* ANTES: */
.modal-card {
  background: var(--paper-white);
  border-radius: 16px;
  max-width: 760px;
  width: 90%;
  max-height: 90vh;
  overflow-y: auto;
  padding: 32px;
  animation: scaleIn 0.25s ease;  /* ← REMOVER esta linha */
}

/* DEPOIS: */
.modal-card {
  background: var(--paper-white);
  border-radius: 16px;
  max-width: 760px;
  width: 90%;
  max-height: 90vh;
  overflow-y: auto;
  padding: 32px;
}
```

Manter a animação apenas no seletor composto (que já existe):
```css
/* Manter esta regra intacta: */
.modal-overlay.visible .modal-card { animation: scaleIn 0.22s ease forwards; }
```

---

## 3. Remover classe obsoleta `.filters`

Localizar e remover completamente o bloco:
```css
/* REMOVER: */
.filters { display: flex; flex-wrap: wrap; gap: 8px; margin-bottom: 20px; overflow-x: auto; }
```

---

## 4. Remover classe obsoleta `.logout-link`

Localizar e remover completamente o bloco:
```css
/* REMOVER: */
.logout-link { font-size: 0.8rem; color: var(--carbon-black); opacity: 0.5; }
.logout-link:hover { opacity: 1; }
```

---

## 5. Adicionar padding-bottom em mobile nos formulários

No bloco `@media (max-width: 768px)`, adicionar:

```css
@media (max-width: 768px) {
  /* ... regras existentes ... */

  /* Espaço para botões flutuantes não sobreporem o conteúdo */
  .form-card,
  .container,
  .container-narrow {
    padding-bottom: 120px;
  }
}
```

---

## 6. Remover `.stats-row.admin` não utilizado

No bloco `@media (max-width: 960px)`, remover a linha:
```css
/* REMOVER: */
.stats-row.admin { grid-template-columns: repeat(2, 1fr); }
```

---

## 7. Remover `@keyframes checkDraw` não utilizado

Localizar e remover completamente:
```css
/* REMOVER: */
@keyframes checkDraw {
  to { stroke-dashoffset: 0; }
}
```

---

## OBSERVAÇÕES

- Não alterar nenhuma outra regra além das listadas
- Após aplicar, verificar visualmente que os modais ainda animam corretamente ao abrir
- O padding-bottom de 120px em mobile é suficiente para dois botões flutuantes empilhados
