# Round 11 — Correções gerais

---

## 1. Secrets do Replit — Assignees ClickUp (AÇÃO MANUAL)

Adicionar nos Secrets do Replit:
```
CLICKUP_ASSIGNEE_GERAL=55140303
CLICKUP_ASSIGNEE_EVENTOS=112032406
```

Nenhuma mudança de código necessária — `clickup.ts` já lê essas variáveis.

---

## 2. `auth.js` — Impersonar no dropdown do usuário

### 2a. Localizar `renderHeader()` e adicionar opção de impersonar para admins

No HTML gerado pelo `renderHeader()`, dentro do dropdown do nome do usuário,
adicionar após os itens existentes:

```js
// Adicionar ao dropdown apenas se isAdmin():
${this.isAdmin() ? `
  <div style="border-top:1px solid rgba(34,27,25,0.08);margin:4px 0;padding-top:4px">
    <div style="padding:6px 12px;font-size:0.72rem;font-weight:700;opacity:0.4;text-transform:uppercase;letter-spacing:0.05em">Admin</div>
    <div id="impersonarWrap" style="padding:4px 8px">
      <div style="font-size:0.78rem;font-weight:600;opacity:0.6;margin-bottom:6px;padding:0 4px">Visualizar como:</div>
      <div style="display:flex;gap:6px">
        <input type="email" id="impersonarEmailInput" placeholder="email@svninvest.com.br"
          style="flex:1;border:1px solid rgba(34,27,25,0.15);border-radius:7px;padding:5px 9px;font-family:'Nunito Sans',sans-serif;font-size:0.78rem;min-width:0"
          onclick="event.stopPropagation()">
        <button onclick="window._impersonar()" style="padding:5px 10px;background:var(--carbon-black);color:white;border:none;border-radius:7px;font-family:'Nunito Sans',sans-serif;font-size:0.78rem;font-weight:600;cursor:pointer;white-space:nowrap">
          Entrar
        </button>
      </div>
    </div>
  </div>
` : ''}
```

### 2b. Adicionar função global `window._impersonar`

No final do script do `auth.js`, após o objeto Auth:

```js
window._impersonar = async function() {
  const input = document.getElementById('impersonarEmailInput');
  const email = input?.value?.trim();
  if (!email || !email.includes('@')) {
    if (input) input.style.borderColor = 'var(--ruby-red)';
    return;
  }
  try {
    const res = await fetch('/api/admin/impersonate', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ email }),
    });
    if (res.ok) {
      // Guardar no sessionStorage para mostrar banner em todas as páginas
      sessionStorage.setItem('svn_impersonate', email);
      window.location.reload();
    } else {
      alert('Não foi possível entrar como esse usuário.');
    }
  } catch { alert('Erro de conexão.'); }
};

window._sairImpersonar = async function() {
  sessionStorage.removeItem('svn_impersonate');
  try {
    await fetch('/api/admin/impersonate/stop', { method: 'POST' });
  } catch {}
  window.location.reload();
};
```

### 2c. Inicializar banner de impersonação em todas as páginas via `auth.js`

No `Auth.init()`, após autenticar, adicionar:

```js
// Verificar e exibir banner de impersonação
const impEmail = sessionStorage.getItem('svn_impersonate');
if (impEmail) {
  // Criar banner se não existir
  if (!document.getElementById('impersonarBanner')) {
    const banner = document.createElement('div');
    banner.id = 'impersonarBanner';
    banner.style.cssText = 'position:fixed;top:0;left:0;right:0;z-index:9999;background:#7c3aed;color:white;padding:8px 20px;display:flex;align-items:center;justify-content:center;gap:10px;font-size:0.82rem;font-weight:600;font-family:"Nunito Sans",sans-serif';
    banner.innerHTML = `
      <svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M17 21v-2a4 4 0 00-4-4H5a4 4 0 00-4 4v2"/><circle cx="9" cy="7" r="4"/></svg>
      Visualizando como: <strong>${impEmail}</strong>
      <button onclick="window._sairImpersonar()" style="background:rgba(255,255,255,0.2);border:none;color:white;padding:3px 10px;border-radius:6px;cursor:pointer;font-family:'Nunito Sans',sans-serif;font-weight:600;font-size:0.78rem;margin-left:6px">
        Sair ✕
      </button>`;
    document.body.prepend(banner);
    // Empurrar conteúdo para baixo
    document.body.style.paddingTop = (parseInt(document.body.style.paddingTop || '0') + 40) + 'px';
  }
}
```

### 2d. Remover bloco de impersonar do painel admin no `dashboard.html`

```html
<!-- REMOVER completamente: -->
<div class="form-card" style="..." id="impersonarCard">
  ...
</div>
```

E remover as funções `impersonar()` e `sairImpersonar()` do script do dashboard,
pois agora ficam em `auth.js`.

---

## 3. `dashboard.html` — Correções no painel admin

### 3a. Corrigir filtro de gráfico (checkboxes não funcionando)

O problema está nos IDs dos gradientes SVG duplicados — quando o canvas
é redesenhado os `linearGradient` IDs conflitam. Corrigir usando IDs únicos
por renderização:

```js
// Em renderAdminGrafico(), substituir o canvas drawing por versão com IDs únicos:
function renderAdminGrafico() {
  if (!adminGraficoData) return;

  const todosItens = adminGraficoModo === 'tipo'
    ? adminGraficoData.porTipo.map(i => ({
        id: i.tipo,
        label: (typeof TIPO_SOLICITACAO_LABELS !== 'undefined' && TIPO_SOLICITACAO_LABELS[i.tipo]) || i.tipo,
        value: i.count
      }))
    : adminGraficoData.porStatus.map(i => ({
        id: i.status,
        label: (STATUS_SOLICITACAO?.find(s => s.id === i.status)?.label) || i.status,
        value: i.count,
        cor: STATUS_SOLICITACAO?.find(s => s.id === i.status)?.bg || null,
      }));

  // Aplicar filtro
  const itens = graficoFiltrosAtivos
    ? todosItens.filter(i => graficoFiltrosAtivos.has(i.id))
    : todosItens;

  // Renderizar checkboxes SEMPRE (mesmo antes de filtrar)
  renderGraficoCheckboxes(todosItens);

  // ... (resto igual ao existente mas usando `itens` filtrados)
}

// Corrigir renderGraficoCheckboxes para usar event handler correto:
function renderGraficoCheckboxes(todosItens) {
  const container = document.getElementById('graficoCheckboxes');
  if (!container) return;
  const CORES = ['#AC3631','#221B19','#4a7c2f','#1e40af','#9333ea','#ea580c','#0891b2','#be123c','#854d0e','#166534'];

  container.innerHTML = todosItens.map((item, i) => {
    const isAtivo = !graficoFiltrosAtivos || graficoFiltrosAtivos.has(item.id);
    const cor = item.cor || CORES[i % CORES.length];
    // Usar data-attribute em vez de onclick inline para evitar problemas de escaping
    return `<label data-item-id="${esc(item.id)}" style="display:inline-flex;align-items:center;gap:6px;padding:5px 12px;border-radius:999px;border:1.5px solid ${isAtivo ? cor : 'rgba(34,27,25,0.12)'};background:${isAtivo ? cor + '18' : 'transparent'};cursor:pointer;font-size:0.78rem;font-weight:600;transition:all 0.15s;user-select:none">
      <input type="checkbox" ${isAtivo ? 'checked' : ''}
        data-id="${esc(item.id)}"
        style="display:none">
      <span style="width:8px;height:8px;border-radius:2px;background:${cor};flex-shrink:0"></span>
      ${esc(item.label)}
    </label>`;
  }).join('');

  // Adicionar event listeners após inserir no DOM
  container.querySelectorAll('label').forEach(label => {
    label.addEventListener('click', () => {
      const id = label.dataset.itemId;
      toggleGraficoFiltro(id);
    });
  });
}
```

### 3b. Campo de busca mais estreito

```js
// Substituir o input da busca expandida:
// DE: style="width:220px"
// PARA: style="width:180px"

// E reduzir o wrapper expandido:
// DE: width:220px
// PARA: width:190px
```

### 3c. Filtro de avaliação como label clicável estilizada

Substituir o checkbox atual por:

```html
<label id="adminAvaliacaoLabel"
  style="display:inline-flex;align-items:center;gap:6px;padding:0 12px;height:32px;border-radius:8px;border:1.5px solid rgba(34,27,25,0.12);background:transparent;cursor:pointer;font-size:0.82rem;font-weight:600;white-space:nowrap;transition:all 0.15s;user-select:none"
  onclick="toggleAvaliacaoFiltro()">
  <input type="checkbox" id="adminFiltroAvaliacao" style="display:none" onchange="loadAdminHistorico()">
  <svg id="adminAvaliacaoStar" width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polygon points="12 2 15.09 8.26 22 9.27 17 14.14 18.18 21.02 12 17.77 5.82 21.02 7 14.14 2 9.27 8.91 8.26 12 2"/></svg>
  Com avaliação
</label>
```

```js
function toggleAvaliacaoFiltro() {
  const cb = document.getElementById('adminFiltroAvaliacao');
  const label = document.getElementById('adminAvaliacaoLabel');
  const star = document.getElementById('adminAvaliacaoStar');
  cb.checked = !cb.checked;
  if (cb.checked) {
    label.style.borderColor = '#f59e0b';
    label.style.background = 'rgba(245,158,11,0.08)';
    star.setAttribute('fill', '#f59e0b');
    star.setAttribute('stroke', '#f59e0b');
  } else {
    label.style.borderColor = 'rgba(34,27,25,0.12)';
    label.style.background = 'transparent';
    star.setAttribute('fill', 'none');
    star.setAttribute('stroke', 'currentColor');
  }
  adminPageHistorico = 1;
  loadAdminHistorico();
}
```

### 3d. Header da tabela mais destacado

```html
<!-- Substituir o <thead> para ter mais peso visual: -->
<thead>
  <tr style="background:var(--carbon-black)">
    <th onclick="sortAdmin('created_at')" style="padding:10px 14px;cursor:pointer;white-space:nowrap;user-select:none">
      <span style="font-weight:700;font-size:0.72rem;text-transform:uppercase;letter-spacing:0.06em;color:rgba(255,255,255,0.7)">
        Data <span id="sortCreated" style="color:white">↓</span>
      </span>
    </th>
    <!-- Repetir para todas as colunas com color:rgba(255,255,255,0.7) -->
  </tr>
</thead>
```

### 3e. Ordenação A-Z/Z-A em todas as colunas

```js
// Substituir sortAdmin() para alternar entre asc/desc com labels:
function sortAdmin(coluna) {
  if (adminOrdemColuna === coluna) {
    adminOrdemDir = adminOrdemDir === 'desc' ? 'asc' : 'desc';
  } else {
    adminOrdemColuna = coluna;
    adminOrdemDir = 'asc'; // começar com A-Z ao selecionar nova coluna
  }

  // Mapa de ID do span de seta para coluna
  const sortSpans = {
    created_at:        'sortCreated',
    user_email:        'sortEmail',
    titulo:            'sortTitulo',
    tipo_solicitacao:  'sortTipo',
    status:            'sortStatus',
  };

  // Limpar todos
  Object.values(sortSpans).forEach(id => {
    const el = document.getElementById(id);
    if (el) el.textContent = '';
  });

  // Marcar ativo
  const activeSpan = document.getElementById(sortSpans[coluna]);
  if (activeSpan) activeSpan.textContent = adminOrdemDir === 'asc' ? ' ↑' : ' ↓';

  adminPageHistorico = 1;
  loadAdminHistorico();
}
```

Atualizar `<thead>` para incluir coluna Solicitante com `id="sortEmail"`:
```html
<th onclick="sortAdmin('user_email')" ...>
  Solicitante <span id="sortEmail"></span>
</th>
```

---

## 4. `solicitacao.html` e `dashboard.html` — Formato de data

### 4a. Atualizar `formatarData()` para o novo formato

```js
// Substituir em ambos os arquivos:
function formatarData(raw) {
  if (!raw) return '';
  const meses = ['janeiro','fevereiro','março','abril','maio','junho',
                 'julho','agosto','setembro','outubro','novembro','dezembro'];
  let d;
  const matchISO = String(raw).match(/^(\d{4})-(\d{2})-(\d{2})/);
  if (matchISO) {
    d = new Date(parseInt(matchISO[1]), parseInt(matchISO[2])-1, parseInt(matchISO[3]));
  } else {
    d = new Date(raw);
  }
  if (isNaN(d.getTime())) return String(raw);
  const dia = d.getDate();
  const mes = meses[d.getMonth()];
  const ano = d.getFullYear();
  const diaFmt = String(dia).padStart(2, '0');
  const mesFmt = String(d.getMonth() + 1).padStart(2, '0');
  return `${dia} de ${mes} de ${ano} (${diaFmt}/${mesFmt}/${ano})`;
}
```

---

## 5. `clickup.ts` — Corrigir acentuação nos labels da descrição

### 5a. Verificar e corrigir `buildDetailsSection()` e `buildResumoSection()`

```ts
// Substituir labels sem acento por versões corretas:
"E mail"          → "E-mail"
"E-mail marketing"  → correto ✅
"Mini bio"         → "Mini bio" ✅
"Codigo do assessor" → "Código do assessor"
"Contrato social"   → correto ✅
"Publico-alvo"      → "Público-alvo"
"Natureza"          → correto ✅
"Nivel de maturidade" → "Nível de maturidade"
"Titulo do evento"  → "Título do evento"
"Solicitacoes"      → "Solicitações"
"Descricao"         → "Descrição"
```

### 5b. Em `buildRequesterSection()`:

```ts
// Garantir que o label usa hífen:
items.push(`• E-mail: ${user.email}`);
// (não "E mail" nem "Email")
```

---

## 6. `style.css` — Botão WhatsApp adaptável

### 6a. Remover `bottom: 138px` fixo do `.float-whatsapp`

```css
/* Remover bottom fixo do CSS: */
.float-whatsapp {
  /* bottom: 138px; ← REMOVER */
  /* manter todo o resto */
}
```

### 6b. Adicionar script inline nas páginas para calcular posição dinâmica

Em cada página que tem o botão WhatsApp, adicionar logo após o elemento:

```html
<script>
(function() {
  function posicionarWhatsApp() {
    const wa = document.querySelector('.float-whatsapp');
    if (!wa) return;
    // Coletar todos os outros botões flutuantes visíveis
    const outros = [...document.querySelectorAll('.float-home')]
      .filter(el => el !== wa && getComputedStyle(el).display !== 'none');
    if (outros.length === 0) {
      wa.style.bottom = '78px'; // só tem início
    } else {
      // Pegar o bottom mais alto entre os outros botões flutuantes
      const alturas = outros.map(el => parseInt(el.style.bottom || getComputedStyle(el).bottom) || 20);
      const maiorAltura = Math.max(...alturas);
      wa.style.bottom = (maiorAltura + 58) + 'px';
    }
  }
  // Rodar após render
  if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', posicionarWhatsApp);
  } else {
    posicionarWhatsApp();
  }
})();
</script>
```

---

## 7. `form-pagina-assessores.html` — Atualizar link NPS

```html
<!-- Localizar o link do botão "Acessar NPS" e substituir o href: -->

<!-- DE: -->
href="https://svninvest-my.sharepoint.com/:x:/g/personal/fabio_santos_svninvest_com_br/..."

<!-- PARA: -->
href="https://svninvest-my.sharepoint.com/:x:/g/personal/eduardo_filho_svninvest_com_br/IQBsd2rvk_KKQ44PVtzuxQrSAS6m-YB05OaB0m11B0pvc7k?wdExp=TEAMS-TREATMENT&web=1&isSPOFile=1&ovuser=91608a81-cb4c-4123-8e4f-ffc75f8c4b6c%2Cjoao.sardeto%40svninvest.com.br&clickparams=eyJBcHBOYW1lIjoiVGVhbXMtRGVza3RvcCIsIkFwcFZlcnNpb24iOiI1MC8yNjAzMTIyMzAyMCIsIkhhc0ZlZGVyYXRlZFVzZXIiOmZhbHNlfQ%3D%3D"
```

---

## OBSERVAÇÕES

- `clickup.ts` e `forms.ts` precisam de build após edições
- `auth.js`, `dashboard.html`, `solicitacao.html`, `style.css` e
  `form-pagina-assessores.html` não precisam de build
- Os IDs `CLICKUP_ASSIGNEE_GERAL=55140303` e
  `CLICKUP_ASSIGNEE_EVENTOS=112032406` devem ser configurados
  nos Secrets do Replit — essa é a causa raiz das tasks sem responsável
- O banner de impersonação aparece em todas as páginas via `auth.js`,
  sem necessidade de adicionar HTML em cada página individualmente
