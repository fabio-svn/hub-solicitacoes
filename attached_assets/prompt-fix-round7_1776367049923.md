# Correções — Round 7

---

## 1. `form-eventos.html`

### 1a. CRÍTICO — Corrigir case acentuado em `buildCampoField()`

Localizar o switch/case dentro de `buildCampoField` e substituir:
```js
// DE:
case 'link-transmissão': return `<div class="field"><label>${campo.label}</label><input type="url" id="linkTransmissao"></div>`;

// PARA:
case 'link-transmissao': return `<div class="field"><label>${campo.label}</label><input type="url" id="linkTransmissao"></div>`;
```

### 1b. Substituir `alert()` da maturidade em `validateStep2()`

```js
// DE:
if (!mat) {
  alert('Selecione um nível de maturidade para continuar.');
  return false;
}

// PARA:
if (!mat) {
  const cards = document.querySelectorAll('#step2 .radio-card');
  cards.forEach(c => c.style.outline = '2px solid var(--ruby-red)');
  setTimeout(() => cards.forEach(c => c.style.outline = ''), 1200);
  return false;
}
```

### 1c. Substituir `alert()` de erro de envio em `submitForm()`

Adicionar elemento de erro antes do botão Enviar no HTML do step6:
```html
<div id="submitError" class="alert-card alert-danger" style="display:none;margin-bottom:16px"></div>
```

Substituir no JS:
```js
// DE:
else { alert('Erro ao enviar.'); btn.disabled = false; btn.textContent = 'Enviar'; }
} catch (e) { alert('Erro de conexão.'); btn.disabled = false; btn.textContent = 'Enviar'; }

// PARA:
else {
  const errEl = document.getElementById('submitError');
  if (errEl) { errEl.style.display = 'block'; errEl.textContent = 'Erro ao enviar. Tente novamente.'; }
  btn.disabled = false; btn.textContent = 'Enviar';
}
} catch (e) {
  const errEl = document.getElementById('submitError');
  if (errEl) { errEl.style.display = 'block'; errEl.textContent = 'Erro de conexão. Verifique sua internet.'; }
  btn.disabled = false; btn.textContent = 'Enviar';
}
```

### 1d. Corrigir acentuação em `handleLocal()`

```js
// DE:
container.innerHTML = `...<label>Endereco</label>...`;
// PARA:
container.innerHTML = `...<label>Endereço</label>...`;
```

---

## 2. `dashboard.html`

### 2a. Garantir que `temPalestrante` aparece antes dos blocos de palestrante

No `renderDrawerContent()`, antes do loop principal `for (const [key, value] of dadosEntries)`,
adicionar renderização explícita e ordenada do campo temPalestrante e dos blocos:

```js
// Substituir o loop completo por versão com ordem garantida:

// 1. Renderizar todos os campos NÃO-palestrante primeiro (exceto temPalestrante)
let bodyHtml = '';
const dadosEntries = Object.entries(dados);
const palKeysProcessed = new Set();
let temPalestranteHtml = '';
let palestrantesHtml = '';

for (const [key, value] of dadosEntries) {
  if (value === null || value === undefined || value === '') continue;
  if (/^palFoto\d$/.test(key)) continue;

  // Guardar temPalestrante para renderizar na ordem certa
  if (key === 'temPalestrante') {
    const label = DRAWER_FIELD_LABELS[key] || key;
    temPalestranteHtml = `<div class="drawer-field">
      <div class="drawer-field-label">${esc(label)}</div>
      <div class="drawer-field-value">${esc(String(humanizeValue(key, value)))}</div>
    </div>`;
    continue;
  }

  // Agrupar palestrantes numerados
  const palMatch = key.match(/^pal(Svn|Nome|Cargo)(\d)$/);
  if (palMatch) {
    const n = palMatch[2];
    if (palKeysProcessed.has('pal_' + n)) continue;
    palKeysProcessed.add('pal_' + n);

    const nome  = String(dados['palNome'  + n] || '').trim();
    const cargo = String(dados['palCargo' + n] || '').trim();
    const svn   = String(dados['palSvn'   + n] || '').trim();
    if (!nome) continue;

    const svnBadge = svn.toLowerCase() === 'sim'
      ? `<span style="background:var(--ruby-red);color:var(--paper-white);font-size:0.65rem;padding:2px 8px;border-radius:999px;font-weight:700;margin-left:6px">SVN</span>`
      : '';

    palestrantesHtml += `
      <div class="drawer-field" style="background:var(--icon-bg);border-radius:8px;padding:10px 14px;margin-bottom:8px">
        <div style="font-size:0.75rem;opacity:0.45;margin-bottom:4px">Palestrante ${n}</div>
        <div style="font-weight:700;font-size:0.9rem;display:flex;align-items:center">
          ${esc(nome)}${svnBadge}
        </div>
        ${cargo ? `<div style="font-size:0.8rem;opacity:0.6;margin-top:2px">${esc(cargo)}</div>` : ''}
      </div>`;
    continue;
  }

  // Campos normais
  const label = (typeof DRAWER_FIELD_LABELS !== 'undefined' && DRAWER_FIELD_LABELS[key])
    ? DRAWER_FIELD_LABELS[key] : key;

  if (Array.isArray(value)) {
    if (value.length === 0) continue;
    if (typeof value[0] === 'object') {
      bodyHtml += `<div class="drawer-field"><div class="drawer-field-label">${esc(label)}</div>
        ${value.map(dep => `<div style="background:var(--icon-bg);border-radius:8px;padding:10px 12px;margin-top:6px;font-size:0.85rem">
          ${dep.texto ? `<div style="font-style:italic;margin-bottom:4px">${esc(dep.texto)}</div>` : ''}
          ${dep.nome  ? `<div style="font-weight:600;font-size:0.8rem">${esc(dep.nome)}</div>`  : ''}
        </div>`).join('')}</div>`;
    } else {
      bodyHtml += `<div class="drawer-field"><div class="drawer-field-label">${esc(label)}</div><div class="drawer-field-value">${esc(humanizeValue(key, value))}</div></div>`;
    }
    continue;
  }
  if (typeof value === 'object') continue;
  bodyHtml += `<div class="drawer-field">
    <div class="drawer-field-label">${esc(label)}</div>
    <div class="drawer-field-value">${esc(String(humanizeValue(key, value)))}</div>
  </div>`;
}

// Montar corpo final: campos normais + temPalestrante + blocos de palestrante
if (temPalestranteHtml || palestrantesHtml) {
  bodyHtml += temPalestranteHtml + palestrantesHtml;
}

document.getElementById('drawerBody').innerHTML = bodyHtml || '<p style="opacity:0.5">Sem dados adicionais</p>';
```

### 2b. Limitar tamanho do `itemsCache` ao paginar

Substituir a função `goPage`:
```js
function goPage(tab, page) {
  // Limpar cache da aba atual ao paginar para evitar crescimento indefinido
  Object.keys(itemsCache).forEach(k => {
    if (k.startsWith(tab + '_')) delete itemsCache[k];
  });
  pages[tab] = page;
  loadList(tab);
}
```

---

## 3. `config.js`

### 3a. Adicionar "cancelado" como status excepcional no fluxo de eventos

```js
// Em FLUXOS_ETAPAS["eventos"], adicionar após "em-espera":
{ id: "cancelado", label: "Cancelado", visivel: false },
```

---

## 4. `clickup.ts`

### 4a. Mover `IBGE_SIGLA_MAP` para constante no topo do arquivo

Adicionar junto com `IBGE_STATE_MAP` no topo (após as importações):
```ts
const IBGE_SIGLA_MAP: Record<string, string> = {
  "12":"AC","27":"AL","16":"AP","13":"AM","29":"BA","23":"CE","53":"DF",
  "32":"ES","52":"GO","21":"MA","51":"MT","50":"MS","31":"MG","15":"PA",
  "25":"PB","41":"PR","26":"PE","22":"PI","33":"RJ","24":"RN","43":"RS",
  "11":"RO","14":"RR","42":"SC","35":"SP","28":"SE","17":"TO",
};
```

Remover a declaração inline dentro de `setEventosCustomFields`:
```ts
// REMOVER este bloco de dentro da função:
const IBGE_SIGLA_MAP: Record<string, string> = {
  "12":"AC", // ... etc
};
```

---

## 5. `forms.ts` — Implementar filtros e rate limiting

### 5a. Implementar filtros `natureza` e `subtipo_filter`

No GET `/solicitacoes`, adicionar após o bloco `if (req.query.status)`:

```ts
// Filtro por natureza (campo JSONB)
if (req.query.natureza) {
  const natureza = String(req.query.natureza);
  if (natureza === 'presencial' || natureza === 'online') {
    conditions.push(sql`${solicitacoesTable.dados}->>'natureza' = ${natureza}`);
  }
}

// Filtro por tipo/subtipo (prefixo do tipo_solicitacao)
if (req.query.subtipo_filter) {
  const subtipoFilter = String(req.query.subtipo_filter).replace(/[^a-z0-9-]/g, '');
  if (subtipoFilter) {
    conditions.push(sql`${solicitacoesTable.tipo_solicitacao} LIKE ${subtipoFilter + '%'}`);
  }
}
```

### 5b. Implementar rate limiting simples

Adicionar antes de `router.post`:
```ts
const submissionCounts = new Map<string, { count: number; resetAt: number }>();

function checkRateLimit(email: string): boolean {
  const now = Date.now();
  const entry = submissionCounts.get(email);
  if (!entry || now > entry.resetAt) {
    submissionCounts.set(email, { count: 1, resetAt: now + 60 * 60 * 1000 });
    return true;
  }
  if (entry.count >= 10) return false;
  entry.count++;
  return true;
}
```

Adicionar no início do handler do POST, após `const user = req.session.user!;`:
```ts
if (!checkRateLimit(user.email)) {
  res.status(429).json({ error: "Muitas solicitações. Tente novamente em 1 hora." });
  return;
}
```

### 5c. Corrigir mensagens de erro com acentuação

```ts
// Substituir todas as ocorrências:
"tipo_solicitacao e obrigatorio"    → "tipo_solicitacao é obrigatório"
"tipo_solicitacao invalido"          → "tipo_solicitacao inválido"
"maturidade deve ser 1, 2 ou 3"    → correto ✅
"Campo obrigatorio ausente:"         → "Campo obrigatório ausente:"
"ID invalido"                        → "ID inválido"
"Solicitacao nao encontrada"         → "Solicitação não encontrada"
"Erro ao processar solicitacao"      → "Erro ao processar solicitação"
"Erro ao listar solicitacoes"        → "Erro ao listar solicitações"
"Erro ao obter estatisticas"         → "Erro ao obter estatísticas"
"Erro ao verificar status"           → correto ✅
```

---

## OBSERVAÇÕES

- Após editar `forms.ts` e `clickup.ts`, rodar build:
  `cd artifacts/api-server && pnpm run build`
- `config.js`, `dashboard.html` e `form-eventos.html` não precisam de build
- O `globals.css` do index 30 (com `@import "tailwindcss"`) é do
  mockup-sandbox do Replit — confirmar que não está sendo importado
  em nenhum arquivo HTML do hub
- A duplicidade de `UNIDADES_ENDERECOS` entre `clickup.ts` e `config.js`
  não tem solução técnica imediata (backends separados), mas documentar:
  sempre que atualizar uma unidade, atualizar em ambos os arquivos
