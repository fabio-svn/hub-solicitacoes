# Round 5 — Ajustes Hub SVN

---

## 1. DRAWER — Fluxo de etapas mais compacto + drawer mais largo

### 1a. Aumentar largura do drawer no `style.css`

```css
/* Substituir: */
.drawer { right: -440px; width: 420px; ... }
/* Por: */
.drawer { right: -500px; width: 480px; ... }

/* E no mobile: */
@media (max-width: 768px) {
  .drawer { width: 100%; right: -100%; }
}
```

### 1b. Redesenhar o fluxo de etapas no `openDrawer()`

Substituir a lógica de renderização do fluxo por uma versão compacta onde apenas a etapa ativa exibe o nome completo — as demais mostram só o círculo com tooltip ao hover:

```js
fluxoEl.innerHTML = `
  <div style="margin-bottom:4px">
    <p style="font-size:0.7rem;font-weight:600;letter-spacing:0.07em;text-transform:uppercase;opacity:0.4;margin-bottom:14px">Andamento da solicitação</p>
    <div style="display:flex;align-items:center;gap:0">
      ${etapasVisiveis.map((etapa, idx) => {
        const isConcluida = idx < idxAtual;
        const isAtual = idx === idxAtual;
        const isUltima = idx === etapasVisiveis.length - 1;

        const corCirculo = isConcluida
          ? '#065f46' : isAtual
          ? getStatusColor(etapa.id).bg
          : 'rgba(34,27,25,0.1)';

        const corBorda = isConcluida
          ? '#065f46' : isAtual
          ? getStatusColor(etapa.id).text
          : 'rgba(34,27,25,0.15)';

        const icone = isConcluida
          ? `<svg width="9" height="9" viewBox="0 0 24 24" fill="none" stroke="white" stroke-width="3"><polyline points="20 6 9 17 4 12"/></svg>`
          : isAtual
          ? `<div style="width:6px;height:6px;border-radius:50%;background:${getStatusColor(etapa.id).text}"></div>`
          : '';

        const circulo = `
          <div title="${etapa.label}" style="
            width:22px;height:22px;border-radius:50%;
            background:${isConcluida ? '#065f46' : isAtual ? getStatusColor(etapa.id).bg : 'transparent'};
            border:2px solid ${corBorda};
            display:flex;align-items:center;justify-content:center;
            flex-shrink:0;position:relative;z-index:1;cursor:default;
            transition:0.2s
          ">${icone}</div>`;

        const linha = !isUltima ? `
          <div style="flex:1;height:2px;background:${isConcluida ? '#065f46' : 'rgba(34,27,25,0.1)'};min-width:8px"></div>
        ` : '';

        const labelAtual = isAtual ? `
          <div style="position:absolute;top:28px;left:50%;transform:translateX(-50%);white-space:nowrap;font-size:0.7rem;font-weight:700;color:${getStatusColor(etapa.id).text}">
            ${etapa.label}
          </div>
        ` : '';

        return `
          <div style="display:flex;align-items:center;flex:${isUltima ? '0' : '1'};position:relative">
            <div style="position:relative">
              ${circulo}
              ${labelAtual}
            </div>
            ${linha}
          </div>`;
      }).join('')}
    </div>
    <div style="height:24px"></div>
  </div>`;
```

### 1c. Adicionar função `getStatusColor()` no `dashboard.html`

Adicionar no `<script>` do `dashboard.html`:

```js
function getStatusColor(statusId) {
  const map = {
    'recebido':               { bg: '#e8f4ea', text: '#2d7a3a' },
    'alinhamentos':           { bg: '#e8f0fb', text: '#1a56a0' },
    'em-andamento':           { bg: '#fff3e0', text: '#b45309' },
    'cotacao-aprovacao':      { bg: '#fef3c7', text: '#92660a' },
    'aguardando-pagamento':   { bg: '#fce8f3', text: '#9d174d' },
    'aguardando-finalizacao': { bg: '#ede9fe', text: '#5b21b6' },
    'concluido':              { bg: '#d1fae5', text: '#065f46' },
    'reprovado':              { bg: '#fee2e2', text: '#991b1b' },
    'cancelado':              { bg: '#fee2e2', text: '#991b1b' },
    'em-espera':              { bg: '#f1f5f9', text: '#475569' },
    // Status legados
    'em-analise':             { bg: '#fff3e0', text: '#b45309' },
    'em-producao':            { bg: '#fff3e0', text: '#b45309' },
    'aguardando':             { bg: '#fef3c7', text: '#92660a' },
    'aguardando-rh':          { bg: '#fce8f3', text: '#9d174d' },
  };
  return map[statusId] || { bg: '#f1f5f9', text: '#475569' };
}
```

---

## 2. DRAWER E DASHBOARD — Cores dos badges de status

### 2a. Atualizar `STATUS_SOLICITACAO` no `config.js`

Substituir o array `STATUS_SOLICITACAO` por versão com cores inline (as cores agora são hexadecimais diretos, não variáveis CSS, para funcionar nos badges inline):

```js
const STATUS_SOLICITACAO = [
  { id: "recebido",               label: "Recebido",                   bg: "#e8f4ea", text: "#2d7a3a"  },
  { id: "alinhamentos",           label: "Alinhamentos",               bg: "#e8f0fb", text: "#1a56a0"  },
  { id: "em-analise",             label: "Em análise",                 bg: "#fff3e0", text: "#b45309"  },
  { id: "em-andamento",           label: "Em andamento",               bg: "#fff3e0", text: "#b45309"  },
  { id: "em-producao",            label: "Em produção",                bg: "#fff3e0", text: "#b45309"  },
  { id: "cotacao-aprovacao",      label: "Em cotação / aprovação",     bg: "#fef3c7", text: "#92660a"  },
  { id: "aguardando",             label: "Aguardando informação",      bg: "#fef3c7", text: "#92660a"  },
  { id: "aguardando-rh",          label: "Aguardando aprovação do RH", bg: "#fce8f3", text: "#9d174d"  },
  { id: "aguardando-pagamento",   label: "Aguardando pagamento",       bg: "#fce8f3", text: "#9d174d"  },
  { id: "aguardando-finalizacao", label: "Aguardando finalização",     bg: "#ede9fe", text: "#5b21b6"  },
  { id: "concluido",              label: "Concluído",                  bg: "#d1fae5", text: "#065f46"  },
  { id: "reprovado",              label: "Reprovado / Cancelado",      bg: "#fee2e2", text: "#991b1b"  },
  { id: "cancelado",              label: "Cancelado",                  bg: "#fee2e2", text: "#991b1b"  },
  { id: "em-espera",              label: "Em espera",                  bg: "#f1f5f9", text: "#475569"  },
];
```

### 2b. Atualizar renderização dos badges no `dashboard.html`

Na função `renderList()`, substituir o badge de status por:

```js
// Substituir:
<span class="badge" style="background:var(${statusObj.cor || '--carbon-black'});color:var(--paper-white)">${esc(statusObj.label)}</span>

// Por:
<span class="badge" style="background:${statusObj.bg || '#f1f5f9'};color:${statusObj.text || '#475569'}">${esc(statusObj.label)}</span>
```

Na função `openDrawer()`, substituir os badges do cabeçalho por:

```js
// Substituir:
<span class="badge" style="background:var(${statusObj.cor || '--carbon-black'});color:var(--paper-white)">${esc(statusObj.label)}</span>

// Por:
<span class="badge" style="background:${statusObj.bg || '#f1f5f9'};color:${statusObj.text || '#475569'}">${esc(statusObj.label)}</span>
```

---

## 3. DRAWER — Labels legíveis para materiais e valores internos

Na função `openDrawer()` do `dashboard.html`, adicionar um mapeamento de valores internos para labels legíveis antes de renderizar o drawer. Isso resolve "pacote-padrao" → "Pacote Padrão", "presencial" → "Presencial", etc.

Adicionar esta função no `<script>` do `dashboard.html`:

```js
function humanizeValue(key, value) {
  // Mapas de valores internos para labels legíveis
  const maps = {
    natureza: { presencial: 'Presencial', online: 'Online' },
    maturidade: { 1: 'Tenho a maioria das informações', 2: 'Tenho algumas informações', 3: 'Ainda estou estruturando' },
    localEvento: { unidade: 'Unidade SVN', externo: 'Local externo', 'nao-definido': 'Não definido' },
    publico: {
      'Fechado para convidados': 'Fechado para convidados',
      'Aberto ao publico em geral': 'Aberto ao público em geral',
    },
  };

  // Mapeamento de ids de materiais para labels
  const materiaisMap = {};
  if (typeof ITENS_MATERIAIS !== 'undefined') {
    ITENS_MATERIAIS.forEach(i => { materiaisMap[i.id] = i.label; });
  }
  if (typeof ITENS_MATERIAIS_ONLINE !== 'undefined') {
    ITENS_MATERIAIS_ONLINE.forEach(i => { materiaisMap[i.id] = i.label; });
  }

  // Arrays de materiais
  if (key === 'materiais' && Array.isArray(value)) {
    return value.map(v => materiaisMap[v] || v).join(', ');
  }

  // Arrays de selos
  if (key === 'selos' && Array.isArray(value)) {
    if (typeof SELOS_ASSESSOR !== 'undefined') {
      const selosMap = {};
      SELOS_ASSESSOR.forEach(s => { selosMap[s.id] = s.label; });
      return value.map(v => selosMap[v] || v).join(' · ');
    }
  }

  // Lookup por chave
  if (maps[key] && maps[key][value] !== undefined) {
    return maps[key][value];
  }

  // Valores genéricos com traço (ids internos como "pacote-padrao")
  if (typeof value === 'string' && value.includes('-') && !value.includes(' ')) {
    // Verificar se é um id de material
    if (materiaisMap[value]) return materiaisMap[value];
  }

  return value;
}
```

Na função `openDrawer()`, ao renderizar cada campo, envolver o valor em `humanizeValue()`:

```js
// Substituir:
<div class="drawer-field-value">${esc(String(value))}</div>

// Por:
<div class="drawer-field-value">${esc(String(humanizeValue(key, value)))}</div>
```

E para arrays simples:
```js
// Substituir:
<div class="drawer-field-value">${value.map(v => esc(String(v))).join(' · ')}</div>

// Por:
<div class="drawer-field-value">${esc(humanizeValue(key, value))}</div>
```

---

## 4. DRAWER — Corrigir lentidão e falha ao abrir

O drawer demora ou não abre porque a função `openDrawer()` faz um fetch para `/api/solicitacoes/:id/status` a cada clique — se o servidor demorar ou o endpoint falhar, o drawer fica travado aguardando.

### 4a. Tornar o fetch de status não-bloqueante

Substituir o início da função `openDrawer()` por:

```js
async function openDrawer(item) {
  // Abrir o drawer IMEDIATAMENTE, sem aguardar o fetch de status
  document.getElementById('drawerOverlay').classList.add('visible');
  document.getElementById('drawer').classList.add('open');

  // Renderizar com os dados que já temos em cache
  renderDrawerContent(item);

  // Atualizar status em background (sem bloquear a abertura)
  try {
    const syncRes = await fetch('/api/solicitacoes/' + item.id + '/status');
    if (syncRes.ok) {
      const syncData = await syncRes.json();
      if (syncData.updated && syncData.status !== item.status) {
        item.status = syncData.status;
        // Re-renderizar apenas o cabeçalho com o novo status
        renderDrawerContent(item);
      }
    }
  } catch {}
}
```

### 4b. Extrair renderização para função separada

```js
function renderDrawerContent(item) {
  let dados = {};
  try {
    dados = typeof item.dados === 'string' ? JSON.parse(item.dados) : (item.dados || {});
  } catch { dados = {}; }

  const titulo = dados.nomeEvento || dados.tituloEvento || dados.titulo || dados.nomeCompleto || item.tipo_solicitacao;
  const tipoLabel = TIPO_SOLICITACAO_LABELS[item.tipo_solicitacao] || item.tipo_solicitacao;
  const statusObj = STATUS_SOLICITACAO.find(s => s.id === item.status) || { label: item.status, bg: '#f1f5f9', text: '#475569' };

  document.getElementById('drawerTitle').textContent = titulo;
  document.getElementById('drawerBadges').innerHTML = `
    <span class="badge" style="background:var(--carbon-black);color:var(--paper-white)">${esc(tipoLabel)}</span>
    <span class="badge" style="background:${statusObj.bg};color:${statusObj.text}">${esc(statusObj.label)}</span>
  `;

  // Fluxo de etapas
  const tipoBase = item.tipo_solicitacao === 'eventos' ? 'eventos' : '_default';
  const fluxo = (typeof FLUXOS_ETAPAS !== 'undefined')
    ? (FLUXOS_ETAPAS[item.tipo_solicitacao] || FLUXOS_ETAPAS[tipoBase] || FLUXOS_ETAPAS['_default'])
    : [];

  const statusAtual = item.status;
  const etapasVisiveis = fluxo.filter(e => e.visivel);
  const etapaExcepcional = fluxo.find(e => !e.visivel && e.id === statusAtual);
  const idxAtual = etapasVisiveis.findIndex(e => e.id === statusAtual);

  const fluxoEl = document.getElementById('drawerFluxo');
  if (fluxoEl) {
    if (etapaExcepcional) {
      fluxoEl.innerHTML = `
        <div style="background:rgba(159,63,55,0.08);border:1px solid rgba(159,63,55,0.2);border-radius:8px;padding:10px 14px;font-size:0.85rem;font-weight:600;color:var(--ruby-red);display:flex;align-items:center;gap:8px">
          <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="10"/><line x1="12" y1="8" x2="12" y2="12"/><line x1="12" y1="16" x2="12.01" y2="16"/></svg>
          ${etapaExcepcional.label}
        </div>`;
    } else if (etapasVisiveis.length > 0) {
      fluxoEl.innerHTML = `
        <p style="font-size:0.7rem;font-weight:600;letter-spacing:0.07em;text-transform:uppercase;opacity:0.4;margin-bottom:14px">Andamento da solicitação</p>
        <div style="display:flex;align-items:center;gap:0;padding-bottom:28px">
          ${etapasVisiveis.map((etapa, idx) => {
            const isConcluida = idx < idxAtual;
            const isAtual = idx === idxAtual;
            const isUltima = idx === etapasVisiveis.length - 1;
            const cor = getStatusColor(etapa.id);

            const bg = isConcluida ? '#065f46' : isAtual ? cor.bg : 'transparent';
            const border = isConcluida ? '#065f46' : isAtual ? cor.text : 'rgba(34,27,25,0.15)';
            const icone = isConcluida
              ? `<svg width="9" height="9" viewBox="0 0 24 24" fill="none" stroke="white" stroke-width="3"><polyline points="20 6 9 17 4 12"/></svg>`
              : isAtual ? `<div style="width:6px;height:6px;border-radius:50%;background:${cor.text}"></div>` : '';
            const labelAtual = isAtual
              ? `<div style="position:absolute;top:28px;left:50%;transform:translateX(-50%);white-space:nowrap;font-size:0.68rem;font-weight:700;color:${cor.text}">${etapa.label}</div>`
              : '';

            return `
              <div style="display:flex;align-items:center;flex:${isUltima ? '0' : '1'};position:relative">
                <div style="position:relative">
                  <div title="${etapa.label}" style="width:22px;height:22px;border-radius:50%;background:${bg};border:2px solid ${border};display:flex;align-items:center;justify-content:center;flex-shrink:0;position:relative;z-index:1">${icone}</div>
                  ${labelAtual}
                </div>
                ${!isUltima ? `<div style="flex:1;height:2px;background:${isConcluida ? '#065f46' : 'rgba(34,27,25,0.1)'};min-width:8px"></div>` : ''}
              </div>`;
          }).join('')}
        </div>`;
    } else {
      fluxoEl.innerHTML = '';
    }
  }

  // Campos do formulário
  let bodyHtml = '';
  for (const [key, value] of Object.entries(dados)) {
    if (value === null || value === undefined || value === '') continue;
    const label = (typeof DRAWER_FIELD_LABELS !== 'undefined' && DRAWER_FIELD_LABELS[key]) ? DRAWER_FIELD_LABELS[key] : key;

    if (Array.isArray(value)) {
      if (value.length === 0) continue;
      if (typeof value[0] === 'object') {
        bodyHtml += `<div class="drawer-field"><div class="drawer-field-label">${esc(label)}</div>
          ${value.map(dep => `<div style="background:var(--icon-bg);border-radius:8px;padding:10px 12px;margin-top:6px;font-size:0.85rem">
            ${dep.texto ? `<div style="font-style:italic;margin-bottom:4px">${esc(dep.texto)}</div>` : ''}
            ${dep.nome ? `<div style="font-weight:600;font-size:0.8rem">${esc(dep.nome)}</div>` : ''}
          </div>`).join('')}</div>`;
      } else {
        bodyHtml += `<div class="drawer-field"><div class="drawer-field-label">${esc(label)}</div><div class="drawer-field-value">${esc(humanizeValue(key, value))}</div></div>`;
      }
      continue;
    }
    if (typeof value === 'object') continue;
    bodyHtml += `<div class="drawer-field"><div class="drawer-field-label">${esc(label)}</div><div class="drawer-field-value">${esc(String(humanizeValue(key, value)))}</div></div>`;
  }

  document.getElementById('drawerBody').innerHTML = bodyHtml || '<p style="opacity:0.5">Sem dados adicionais</p>';
  document.getElementById('drawerFooter').textContent = `ID: ${item.id} | Criado em: ${new Date(item.created_at).toLocaleString('pt-BR')}`;
}
```

---

## OBSERVAÇÕES

- A função `renderDrawerContent()` substitui o bloco de renderização que estava dentro de `openDrawer()` — o conteúdo é o mesmo, apenas separado para permitir re-renderização após atualização de status
- O `FLUXOS_ETAPAS['_default']` já está no `config.js` do Round 4 — garantir que foi aplicado antes deste round
- A largura maior do drawer (480px) deve ser testada em mobile para garantir que o override `width: 100%` ainda funciona corretamente

---

## 5. TELA DE SOLICITAÇÕES — Correções visuais da grade

### 5a. Corrigir itens órfãos nas grades (pontos 1 e 2)

No `style.css`, substituir o grid da `.selection-grid` para preencher automaticamente sem deixar células vazias:

```css
.selection-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(260px, 1fr));
  gap: 12px;
}
```

Isso elimina o problema de itens órfãos em categorias com 5 ou 7 itens — os cards se distribuem automaticamente ocupando o espaço disponível sem deixar células vazias.

### 5b. Adicionar subtexto de contexto abaixo do título

No `solicitacoes.html`, na função `init()`, após `renderCategories()`, adicionar subtexto dinâmico com contagem de categorias e itens disponíveis:

```js
// Adicionar após renderCategories():
const totalItens = CATEGORIAS_SOLICITACAO.reduce((acc, cat) => acc + cat.itens.length, 0);
const totalAtivos = CATEGORIAS_SOLICITACAO.reduce((acc, cat) => acc + cat.itens.filter(i => i.ativo).length, 0);
const totalCats = CATEGORIAS_SOLICITACAO.length;

const subtexto = document.createElement('p');
subtexto.style.cssText = 'font-size:0.8rem;color:var(--carbon-black);opacity:0.4;margin-bottom:28px;margin-top:-8px;font-weight:400';
subtexto.textContent = `${totalCats} categorias · ${totalAtivos} de ${totalItens} disponíveis`;

const titulo = document.querySelector('.container h1');
if (titulo) titulo.insertAdjacentElement('afterend', subtexto);
```

### 5c. Encurtar texto do botão flutuante

No `solicitacoes.html`, localizar o botão flutuante e substituir o texto:

```html
<!-- Substituir: -->
<span>Voltar à tela inicial</span>
<!-- Por: -->
<span>Início</span>
```


---

## 6. PERFORMANCE FRONTEND — Preload de fontes e lazy loading de imagens

### 6a. Substituir @import de fontes por preload no `<head>` de todos os HTMLs

O `style.css` usa `@import url('https://fonts.googleapis.com...')` que bloqueia a renderização. Substituir em **todos os arquivos HTML** (`index.html`, `solicitacoes.html`, `dashboard.html`, `form-eventos.html`, `form-pagina-assessores.html`, `form-apresentacoes.html`, `form-artes-divulgacao.html`, `form-atualizacao-material.html`, `form-criacao-pdf.html`, `thankyou.html`, `admin.html`) pelo seguinte bloco no `<head>`, antes do `<link rel="stylesheet" href="style.css">`:

```html
<!-- Preconnect para reduzir latência DNS -->
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<!-- Carregar fontes de forma não-bloqueante -->
<link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Nunito+Sans:opsz,wght@6..12,300;6..12,400;6..12,600;6..12,700&family=Taviraj:wght@300;400&display=swap" media="print" onload="this.media='all'">
<!-- Fallback para browsers sem JS -->
<noscript><link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Nunito+Sans:opsz,wght@6..12,300;6..12,400;6..12,600;6..12,700&family=Taviraj:wght@300;400&display=swap"></noscript>
```

E remover do `style.css` a linha:
```css
/* REMOVER: */
@import url('https://fonts.googleapis.com/css2?family=Nunito+Sans...');
```

O truque `media="print" onload="this.media='all'"` faz o browser carregar a fonte em background sem bloquear o render — a página aparece imediatamente com a fonte fallback e troca para Nunito Sans/Taviraj assim que carregarem.

### 6b. Adicionar `loading="lazy"` em todas as imagens não-críticas

Em todos os arquivos HTML, adicionar `loading="lazy"` nas seguintes imagens:

**`form-pagina-assessores.html`:**
```html
<!-- Logos dos selos — adicionar loading="lazy" em cada <img> gerada dinamicamente -->
<!-- Na função que renderiza os selos, garantir: -->
img.loading = 'lazy';

<!-- Foto do palestrante no preview: -->
<img id="previewPhoto" loading="lazy" ...>
```

**`form-eventos.html`:**
```html
<!-- Foto do palestrante: -->
<img class="speaker-avatar" loading="lazy" ...>

<!-- Imagens do popup do pacote padrão — já tem lazy loading implementado, manter como está ✅ -->
```

**`dashboard.html` e `admin.html`:**
```html
<!-- Logo no header — NÃO adicionar lazy (é crítica, precisa aparecer imediatamente) -->
<!-- Demais imagens dinâmicas: adicionar loading="lazy" ao criar elementos via JS: -->
img.loading = 'lazy';
```

**Regra geral para imagens criadas via JS** em todos os formulários:
```js
// Sempre que criar um elemento <img> via JS, adicionar:
const img = document.createElement('img');
img.loading = 'lazy'; // adicionar esta linha
img.src = url;
```

### 6c. Lazy load da aba inativa no dashboard

No `dashboard.html`, na função `init()`, carregar apenas a aba ativa inicialmente e adiar o carregamento da outra:

```js
async function init() {
  await _configReady;
  await Auth.init();
  if (!Auth.isAuthenticated()) {
    window.location.href = '/auth/login?redirect=/dashboard.html';
    return;
  }
  Auth.renderHeader(document.getElementById('mainHeader'));
  loadStats();
  loadList('eventos'); // carregar aba ativa imediatamente
  // Carregar aba inativa apenas após 800ms ou quando o usuário clicar nela
  setTimeout(() => loadList('geral'), 800);
}
```

E na função `switchTab()`, forçar reload se a aba ainda não tiver dados:

```js
function switchTab(tab) {
  currentTab = tab;
  document.querySelectorAll('.tab').forEach(t => t.classList.toggle('active', t.dataset.tab === tab));
  document.querySelectorAll('.tab-content').forEach(c => c.classList.toggle('active', c.id === 'tab-' + tab));
  const otherTab = tab === 'eventos' ? 'geral' : 'eventos';
  Object.keys(itemsCache).forEach(k => {
    if (k.startsWith(otherTab + '_')) delete itemsCache[k];
  });
  // Se a lista estiver vazia (ainda não carregou), carregar agora
  const listEl = document.getElementById(tab === 'eventos' ? 'listEventos' : 'listGeral');
  if (listEl && !listEl.hasChildNodes()) loadList(tab);
}
```


---

## 7. DRAWER — Fluxo de etapas em lista vertical (substituir horizontal)

Substituir completamente o bloco de renderização do fluxo dentro de `renderDrawerContent()` pelo layout vertical, que exibe todas as etapas com nome sempre visível:

```js
if (fluxoEl) {
  if (etapaExcepcional) {
    fluxoEl.innerHTML = `
      <div style="background:rgba(159,63,55,0.08);border:1px solid rgba(159,63,55,0.2);border-radius:8px;padding:10px 14px;font-size:0.85rem;font-weight:600;color:var(--ruby-red);display:flex;align-items:center;gap:8px">
        <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="10"/><line x1="12" y1="8" x2="12" y2="12"/><line x1="12" y1="16" x2="12.01" y2="16"/></svg>
        ${etapaExcepcional.label}
      </div>`;
  } else if (etapasVisiveis.length > 0) {
    fluxoEl.innerHTML = `
      <p style="font-size:0.7rem;font-weight:600;letter-spacing:0.07em;text-transform:uppercase;opacity:0.4;margin-bottom:14px">Andamento da solicitação</p>
      <div style="display:flex;flex-direction:column">
        ${etapasVisiveis.map((etapa, idx) => {
          const isConcluida = idx < idxAtual;
          const isAtual = idx === idxAtual;
          const isUltima = idx === etapasVisiveis.length - 1;
          const cor = getStatusColor(etapa.id);

          const circBg = isConcluida ? '#065f46' : isAtual ? cor.bg : 'transparent';
          const circBorder = isConcluida ? '#065f46' : isAtual ? cor.text : 'rgba(34,27,25,0.15)';
          const icone = isConcluida
            ? `<svg width="9" height="9" viewBox="0 0 24 24" fill="none" stroke="white" stroke-width="3"><polyline points="20 6 9 17 4 12"/></svg>`
            : isAtual
            ? `<div style="width:6px;height:6px;border-radius:50%;background:${cor.text}"></div>`
            : '';

          const lblColor = isConcluida ? '#065f46' : isAtual ? cor.text : 'rgba(34,27,25,0.35)';
          const lblWeight = isAtual ? '700' : '400';
          const estadoLabel = isConcluida
            ? `<span style="font-size:10px;color:#065f46;margin-left:auto">Concluído</span>`
            : isAtual
            ? `<span style="font-size:10px;font-weight:600;color:${cor.text};margin-left:auto">Atual</span>`
            : '';

          const connector = !isUltima
            ? `<div style="width:2px;height:10px;background:${isConcluida ? '#065f46' : 'rgba(34,27,25,0.1)'};margin-left:9px;flex-shrink:0"></div>`
            : '';

          return `
            <div style="display:flex;align-items:center;gap:10px">
              <div style="width:20px;height:20px;border-radius:50%;background:${circBg};border:2px solid ${circBorder};display:flex;align-items:center;justify-content:center;flex-shrink:0">
                ${icone}
              </div>
              <div style="flex:1;display:flex;align-items:center;justify-content:space-between">
                <span style="font-size:0.8rem;font-weight:${lblWeight};color:${lblColor};line-height:1.3">${etapa.label}</span>
                ${estadoLabel}
              </div>
            </div>
            ${connector}`;
        }).join('')}
      </div>`;
  } else {
    fluxoEl.innerHTML = '';
  }
}
```
