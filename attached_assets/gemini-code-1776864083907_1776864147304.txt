Atue como um Desenvolvedor Node.js Sênior. Preciso que você faça uma refatoração focada em performance, robustez e limpeza de código morto no meu projeto. É estritamente proibido alterar, remover ou modificar qualquer regra de negócio ou funcionalidade existente. 

Por favor, aplique as seguintes alterações, divididas por arquivos:

1. Modificações Críticas de Backend (Prevenção de OOM e Rate Limit)
- Em `routes/forms.ts`: Mude a configuração do `multer` de `memoryStorage()` para usar o disco temporário (`dest: os.tmpdir()`) para evitar OOM em uploads grandes. Adicione o import `import os from "os";`.
- Em `routes/r2.ts`: Altere a função `uploadToR2` para receber o `file.path` em vez de `file.buffer`. Adicione o import `import fs from "fs";`. Use `fs.createReadStream(file.path)` para enviar o arquivo para o R2 (Stream). Adicione um bloco `try...finally` garantindo a exclusão do arquivo temporário com `await fs.promises.unlink(file.path)`.
- Em `routes/clickup.ts` (na função `setEventosCustomFields`): Corrija o "Falso Batching". Em vez de executar `setClickUpCustomField(...)` no momento do `.push()` no array `fieldPromises`, armazene uma função (thunk) como `() => setClickUpCustomField(...)`. No loop final, execute os lotes com `await Promise.all(batch.map(task => task()));`.
- Em `routes/clickup.ts`: Remova as chaves com acento do objeto `CLICKUP_STATUS_MAP` (ex: "em revisão", "em aprovação"), pois a função de normalização já remove os acentos antes da busca.

2. Correções de Concorrência e Arquitetura de Rotas
- Em `routes/auth.ts`: Na rota `GET /login`, envolva o `res.redirect(authUrl)` dentro de um callback `req.session.save((err) => { ... })` para evitar erros de CSRF (Race Condition) do MSAL.
- Em `routes/auth.ts`: Na rota `GET /logout`, adicione `res.clearCookie('connect.sid');` antes do redirect.
- Em `routes/forms.ts`: Recorte as rotas `POST /admin/impersonate` e `POST /admin/impersonate/stop`.
- Em `routes/admin.ts`: Cole essas duas rotas de impersonate aqui. Envolva a resposta de sucesso (`res.json`) dentro de um `req.session.save()` para garantir a persistência da sessão antes de responder ao front-end.
- Em `app.ts` e `routes/index.ts`: Mova todas as importações e usos de rotas da API (como `formsRouter`, `webhookRouter`) para dentro do `routes/index.ts`. No `app.ts`, mantenha apenas `app.use("/api", router);` e corrija a rota "catch-all" do front-end de `app.get("/{*catchAll}")` para o padrão `app.get("*", ...)`.
- Em `auth.middleware.ts`: Na função `requireRole`, reutilize a lógica do `requireAuth` para checar se o usuário existe, aplicando o princípio DRY.

3. Correções de Frontend (UX e Bugs)
- Em `auth.js` (função `_impersonar`): Altere a busca do dropdown de `document.querySelector('.user-dropdown')` para `document.getElementById('userDropdown')`.
- Em `auth.js` (Event Listener global de cliques em `a[href]`): Adicione uma verificação na condicional para ignorar links com o atributo de download (`|| link.hasAttribute('download')`).
- Em `upload-feedback.js`: Na validação de `options.accept`, melhore a lógica para suportar corretamente MIME types com curingas (como `image/*`) validando contra `f.type`.

4. Limpeza de Código Morto e CSS
- Em `index.html`: Remova a tag `<script src="transitions.js" defer></script>`. O arquivo `transitions.js` pode ser deletado do projeto.
- Em `config.js`: Delete o objeto `DRAWER_FIELD_LABELS` por completo.
- Em `style.css`: Corrija a classe `.admin-tabela` para `.admin-table` nas propriedades da coluna fixa (sticky) de ações. Remova o bloco `@keyframes shimmer`. Otimize a transição da classe `.btn` substituindo `transition: all` pelas propriedades específicas (background, color, transform). Remova todo o CSS referente ao Drawer (classes como `.drawer`, `.drawer-overlay`, etc.).

Execute essas etapas cuidadosamente e, após finalizar, me avise se encontrou algum conflito ou erro de compilação no TypeScript.