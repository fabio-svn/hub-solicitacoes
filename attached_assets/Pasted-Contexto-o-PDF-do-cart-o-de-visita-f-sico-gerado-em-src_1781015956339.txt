Contexto: o PDF do cartão de visita físico é gerado em src/cartao/gerar-cartao.ts (função gerarPdf) com pdfkit. A intenção é: MediaBox = 9,4×5,4cm (com sangria de 0,2cm/lado), TrimBox e CropBox = 9×5cm (corte final), BleedBox = 9,4×5,4cm. Hoje isso é tentado na função interna marcarCorte() cutucando doc.page.dictionary.data dentro de um try/catch — e está falhando silenciosamente, então o PDF sai só com MediaBox 9,4×5,4 e SEM TrimBox. Resultado: ao abrir no Illustrator, a sangria aparece como documento em vez de ficar fora de um artboard de 9×5.

Tarefa:
1. Remover a função marcarCorte() e suas chamadas (a abordagem de poke no dicionário do pdfkit não funciona).
2. Após doc.end() obter o buffer do pdfkit, pós-processar com pdf-lib (já instalado) para setar as caixas de forma determinística em TODAS as páginas, usando as MESMAS constantes já existentes na função (PT_CM, SANGRIA, TRIM_W=255.12, TRIM_H=141.73, MEDIA_W, MEDIA_H). pdf-lib usa assinatura (x, y, width, height):
   - setMediaBox(0, 0, MEDIA_W, MEDIA_H)
   - setBleedBox(0, 0, MEDIA_W, MEDIA_H)
   - setTrimBox(SANGRIA, SANGRIA, TRIM_W, TRIM_H)
   - setCropBox(SANGRIA, SANGRIA, TRIM_W, TRIM_H)   // artboard no Illustrator = 9×5
   Ex.:
     const raw = await done;                  // buffer do pdfkit
     const pdf = await PDFDocument.load(raw);
     for (const page of pdf.getPages()) {
       page.setMediaBox(0, 0, MEDIA_W, MEDIA_H);
       page.setBleedBox(0, 0, MEDIA_W, MEDIA_H);
       page.setTrimBox(SANGRIA, SANGRIA, TRIM_W, TRIM_H);
       page.setCropBox(SANGRIA, SANGRIA, TRIM_W, TRIM_H);
     }
     return Buffer.from(await pdf.save());
3. NÃO mudar o desenho: a arte continua sendo desenhada com SVGtoPDF(doc, svg, SANGRIA, SANGRIA, {width: TRIM_W, height: TRIM_H}) — ela fica corretamente dentro da área de corte, com a sangria como substrato em volta.

Restrições:
- Importar PDFDocument de 'pdf-lib' (sem adicionar dependência nova; já existe).
- pnpm exec tsc -p tsconfig.json --noEmit deve continuar verde.

Aceite: ao abrir um cartão recém-gerado no Illustrator, o artboard é 9×5cm e a sangria de 0,2cm aparece como área fora do corte (não como o documento inteiro). MediaBox 9,4×5,4; TrimBox/CropBox 9×5; BleedBox 9,4×5,4.