# Round 13 — Micro ajustes gerais

---

## 1. `dashboard.html` — Tabela admin sem scroll lateral

### 1a. Remover `min-width:700px` da tabela e ajustar larguras das colunas

```html
<!-- Substituir: -->
<table style="width:100%;min-width:700px;border-collapse:collapse;font-size:0.82rem" class="admin-tabela" id="adminTabela">

<!-- Por: -->
<table style="width:100%;border-collapse:collapse;font-size:0.82rem;table-layout:fixed" class="admin-tabela" id="adminTabela">
```

### 1b. Definir larguras fixas nas colunas do thead

```html
<!-- Adicionar <colgroup> logo após a abertura da <table>: -->
<colgroup>
  <col style="width:90px">   <!-- Data -->
  <col style="width:110px">  <!-- Solicitante -->
  <col style="width:auto">   <!-- Título — ocupa o espaço restante -->
  <col style="width:130px">  <!-- Tipo -->
  <col style="width:52px">   <!-- Status bolinha -->
  <col style="width:80px">   <!-- Avaliação -->
  <col style="width:96px">   <!-- Ações -->
</colgroup>
```

### 1c. Garantir truncamento com tooltip em todas as células de texto

```js
// Em renderAdminHistorico(), atualizar as células de Solicitante, Título e Tipo:

// Solicitante — só username antes do @, tooltip com email completo:
'<td style="padding:8px 10px;overflow:hidden">' +
  '<div style="font-size:0.82rem;font-weight:600;white-space:nowrap;overflow:hidden;text-overflow:ellipsis" ' +
       'title="' + esc(item.user_email || '') + '">' +
    esc(item.user_email?.split('@')[0] || '—') +
  '</div>' +
'</td>'

// Título — truncado com tooltip:
'<td style="padding:8px 10px;overflow:hidden">' +
  '<div style="font-size:0.82rem;white-space:nowrap;overflow:hidden;text-overflow:ellipsis" ' +
       'title="' + esc(item.titulo || '') + '">' +
    esc(item.titulo || '—') +
  '</div>' +
'</td>'

// Tipo — truncado com tooltip:
'<td style="padding:8px 10px;overflow:hidden">' +
  '<span style="font-size:0.78rem;opacity:0.7;white-space:nowrap;overflow:hidden;text-overflow:ellipsis;display:block" ' +
        'title="' + esc(tipoLabel) + '">' +
    esc(tipoLabel) +
  '</span>' +
'</td>'
```

### 1d. Remover wrapper com overflow-x:auto (não precisa mais)

```html
<!-- DE: -->
<div style="overflow-x:auto;border-radius:12px;border:1px solid var(--border-light);position:relative" class="admin-tabela-wrap">

<!-- PARA: -->
<div style="border-radius:12px;border:1px solid var(--border-light);position:relative;overflow:hidden" class="admin-tabela-wrap">
```

---

## 2. `dashboard.html` — Botões Ver e Excluir mais evidentes

### 2a. Substituir `.tabela-acao-btn` por versão com fundo e hover colorido

```js
// Na célula de ações de renderAdminHistorico(), substituir os botões:

// Botão Ver — fundo azul sutil, hover azul mais forte:
'<button style="width:28px;height:28px;border-radius:7px;border:none;cursor:pointer;' +
  'display:inline-flex;align-items:center;justify-content:center;' +
  'background:rgba(37,99,235,0.08);color:#2563eb;transition:all 0.15s" ' +
  'title="Ver solicitação" ' +
  'onmouseover="this.style.background=\'rgba(37,99,235,0.18)\';this.style.transform=\'scale(1.08)\'" ' +
  'onmouseout="this.style.background=\'rgba(37,99,235,0.08)\';this.style.transform=\'scale(1)\'" ' +
  'onclick="window.open(\'/solicitacao.html?id=' + item.id + '\',\'_blank\')">' +
  '<svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M1 12s4-8 11-8 11 8 11 8-4 8-11 8-11-8-11-8z"/><circle cx="12" cy="12" r="3"/></svg>' +
'</button>'

// Botão ClickUp — fundo roxo sutil (igual ao existente, só ajustar hover):
// (sem mudança se já tem estilo)

// Botão Excluir — fundo vermelho sutil, hover vermelho intenso + fill no ícone:
'<button style="width:28px;height:28px;border-radius:7px;border:none;cursor:pointer;' +
  'display:inline-flex;align-items:center;justify-content:center;' +
  'background:rgba(220,38,38,0.08);color:#dc2626;transition:all 0.15s" ' +
  'title="Excluir solicitação" ' +
  'onmouseover="this.style.background=\'#dc2626\';this.style.color=\'white\';this.style.transform=\'scale(1.08)\'" ' +
  'onmouseout="this.style.background=\'rgba(220,38,38,0.08)\';this.style.color=\'#dc2626\';this.style.transform=\'scale(1)\'" ' +
  'onclick="confirmarExcluir(' + item.id + ', this)">' +
  '<svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polyline points="3 6 5 6 21 6"/><path d="M19 6l-1 14a2 2 0 01-2 2H8a2 2 0 01-2-2L5 6"/><path d="M10 11v6"/><path d="M14 11v6"/><path d="M9 6V4a1 1 0 011-1h4a1 1 0 011 1v2"/></svg>' +
'</button>'
```

---

## 3. `solicitacao.html` — Badge de responsável mais legível

### No `#solResponsavel`, aumentar contraste e tamanho:

```html
<!-- Substituir o span interno do badge por: -->
<span style="
  display: inline-flex;
  align-items: center;
  gap: 6px;
  padding: 5px 12px 5px 10px;
  background: rgba(34,27,25,0.07);
  border: 1px solid rgba(34,27,25,0.15);
  border-radius: 999px;
  font-size: 0.8rem;
  color: var(--carbon-black);
">
  <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" style="opacity:0.45;flex-shrink:0">
    <path d="M20 21v-2a4 4 0 00-4-4H8a4 4 0 00-4 4v2"/>
    <circle cx="12" cy="7" r="4"/>
  </svg>
  <span style="opacity:0.5;font-weight:500;font-size:0.78rem">Responsável:</span>
  <span class="resp-nome" style="font-weight:700;font-size:0.82rem"></span>
</span>
```

---

## 4. `dashboard.html` — Remover linha após excluir com animação

### Substituir `executarExclusao()` para remover a linha corretamente:

```js
async function executarExclusao() {
  if (!_deleteTargetId) return;
  const btn = document.getElementById('btnConfirmarExclusao');
  btn.disabled = true;
  btn.textContent = 'Excluindo...';

  try {
    const res = await fetch('/api/solicitacoes/' + _deleteTargetId, { method: 'DELETE' });
    if (res.ok) {
      document.getElementById('deleteModal').style.display = 'none';
      if (_deleteTargetRow) {
        const row = _deleteTargetRow;
        const height = row.getBoundingClientRect().height;

        // Fase 1: fade out + slide para direita
        row.style.transition = 'opacity 0.25s ease, transform 0.25s ease';
        row.style.opacity = '0';
        row.style.transform = 'translateX(24px)';

        setTimeout(() => {
          // Fase 2: colapsar altura para remover o espaço
          row.style.transition = 'all 0.2s ease';
          row.style.height = height + 'px';
          row.style.overflow = 'hidden';
          row.style.padding = '0';

          requestAnimationFrame(() => {
            row.style.height = '0';
            row.querySelectorAll('td').forEach(td => {
              td.style.padding = '0';
              td.style.border = 'none';
            });
          });

          setTimeout(() => row.remove(), 220);
        }, 260);
      }
    } else {
      const data = await res.json().catch(() => ({}));
      btn.disabled = false;
      btn.textContent = 'Excluir permanentemente';
      alert(data.error || 'Erro ao excluir. Tente novamente.');
    }
  } catch {
    btn.disabled = false;
    btn.textContent = 'Excluir permanentemente';
    alert('Erro de conexão.');
  }
  _deleteTargetId = null;
  _deleteTargetRow = null;
}
```

---

## 5. `dashboard.html` — Gráfico: dropdowns Por tipo / Por status

### 5a. Substituir os botões de modo do gráfico por dropdowns com checkboxes

```html
<!-- Substituir os botões simples: -->
<!-- DE: -->
<button id="graficoBtnTipo" class="filter-chip active" onclick="switchGrafico('tipo')">Por tipo</button>
<button id="graficoBtnStatus" class="filter-chip" onclick="switchGrafico('status')">Por status</button>

<!-- PARA: -->
<div style="display:flex;gap:6px">
  <div style="position:relative" id="graficoDropdownTipoWrap">
    <button id="graficoBtnTipo"
      onclick="toggleGraficoDropdown('tipo')"
      style="display:inline-flex;align-items:center;gap:5px;padding:5px 12px;border-radius:8px;border:1px solid var(--border-light);background:var(--carbon-black);color:white;font-family:'Nunito Sans',sans-serif;font-size:0.8rem;font-weight:600;cursor:pointer;white-space:nowrap;transition:all 0.15s">
      Por tipo
      <svg id="graficoChevronTipo" width="10" height="10" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" style="transition:transform 0.2s"><polyline points="6 9 12 15 18 9"/></svg>
      <span class="filter-count-badge" id="graficoCountTipo" style="display:none">0</span>
    </button>
    <div id="graficoDropdownTipo" style="display:none;position:absolute;top:36px;right:0;z-index:100;background:#fff;border:1px solid var(--border-light);border-radius:10px;box-shadow:0 4px 20px rgba(0,0,0,.1);padding:8px;min-width:200px;max-height:260px;overflow-y:auto">
      <!-- Preenchido dinamicamente por renderGraficoDropdown('tipo') -->
    </div>
  </div>
  <div style="position:relative" id="graficoDropdownStatusWrap">
    <button id="graficoBtnStatus"
      onclick="toggleGraficoDropdown('status')"
      style="display:inline-flex;align-items:center;gap:5px;padding:5px 12px;border-radius:8px;border:1px solid var(--border-light);background:var(--card-white);color:var(--carbon-black);font-family:'Nunito Sans',sans-serif;font-size:0.8rem;font-weight:600;cursor:pointer;white-space:nowrap;transition:all 0.15s">
      Por status
      <svg id="graficoChevronStatus" width="10" height="10" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" style="transition:transform 0.2s"><polyline points="6 9 12 15 18 9"/></svg>
      <span class="filter-count-badge" id="graficoCountStatus" style="display:none">0</span>
    </button>
    <div id="graficoDropdownStatus" style="display:none;position:absolute;top:36px;right:0;z-index:100;background:#fff;border:1px solid var(--border-light);border-radius:10px;box-shadow:0 4px 20px rgba(0,0,0,.1);padding:8px;min-width:200px">
      <!-- Preenchido dinamicamente por renderGraficoDropdown('status') -->
    </div>
  </div>
</div>
```

### 5b. Funções JS para os dropdowns do gráfico

```js
function toggleGraficoDropdown(modo) {
  // Se clicar no modo inativo → trocar modo e abrir dropdown
  // Se clicar no modo ativo → só abrir/fechar dropdown
  const ddTipo   = document.getElementById('graficoDropdownTipo');
  const ddStatus = document.getElementById('graficoDropdownStatus');
  const chevTipo   = document.getElementById('graficoChevronTipo');
  const chevStatus = document.getElementById('graficoChevronStatus');

  if (modo === 'tipo') {
    const isOpen = ddTipo.style.display === 'block';
    ddTipo.style.display   = isOpen ? 'none' : 'block';
    ddStatus.style.display = 'none';
    chevTipo.style.transform   = isOpen ? 'rotate(0deg)' : 'rotate(180deg)';
    chevStatus.style.transform = 'rotate(0deg)';

    if (!isOpen) {
      if (adminGraficoModo !== 'tipo') switchGrafico('tipo');
      renderGraficoDropdown('tipo');
    }
  } else {
    const isOpen = ddStatus.style.display === 'block';
    ddStatus.style.display = isOpen ? 'none' : 'block';
    ddTipo.style.display   = 'none';
    chevStatus.style.transform = isOpen ? 'rotate(0deg)' : 'rotate(180deg)';
    chevTipo.style.transform   = 'rotate(0deg)';

    if (!isOpen) {
      if (adminGraficoModo !== 'status') switchGrafico('status');
      renderGraficoDropdown('status');
    }
  }
}

function renderGraficoDropdown(modo) {
  if (!adminGraficoData) return;
  const container = document.getElementById(
    modo === 'tipo' ? 'graficoDropdownTipo' : 'graficoDropdownStatus'
  );
  if (!container) return;

  const CORES = ['#AC3631','#221B19','#4a7c2f','#1e40af','#9333ea','#ea580c','#0891b2','#be123c','#854d0e','#166534'];
  const todosItens = modo === 'tipo'
    ? adminGraficoData.porTipo.map(i => ({
        id: i.tipo,
        label: (typeof TIPO_SOLICITACAO_LABELS !== 'undefined' && TIPO_SOLICITACAO_LABELS[i.tipo]) || i.tipo,
        value: i.count,
      }))
    : adminGraficoData.porStatus.map(i => ({
        id: i.status,
        label: STATUS_SOLICITACAO?.find(s => s.id === i.status)?.label || i.status,
        value: i.count,
        cor: STATUS_SOLICITACAO?.find(s => s.id === i.status)?.bg || null,
      }));

  const header = document.createElement('div');
  header.style.cssText = 'display:flex;justify-content:space-between;align-items:center;padding:4px 6px 8px;border-bottom:1px solid var(--border-light);margin-bottom:4px';
  header.innerHTML = `
    <span style="font-size:0.72rem;font-weight:700;opacity:0.4;text-transform:uppercase;letter-spacing:0.05em">Filtrar</span>
    <button onclick="resetGraficoFiltros()" style="font-size:0.75rem;color:var(--ruby-red);background:none;border:none;cursor:pointer;font-family:'Nunito Sans',sans-serif;font-weight:600;padding:0">Todos</button>`;

  container.innerHTML = '';
  container.appendChild(header);

  todosItens.forEach((item, i) => {
    const isAtivo = graficoFiltrosAtivos ? graficoFiltrosAtivos.has(item.id) : true;
    const cor = item.cor || CORES[i % CORES.length];

    const label = document.createElement('label');
    label.className = 'dropdown-checkbox-label';
    label.innerHTML = `
      <input type="checkbox" ${isAtivo ? 'checked' : ''} style="accent-color:${cor}">
      <span style="width:8px;height:8px;border-radius:2px;background:${cor};flex-shrink:0;display:inline-block"></span>
      <span style="flex:1">${esc(item.label)}</span>
      <span style="font-size:0.72rem;opacity:0.4;margin-left:4px">${item.value}</span>`;
    label.querySelector('input').addEventListener('change', () => toggleGraficoFiltro(item.id));
    container.appendChild(label);
  });

  // Atualizar contador no botão
  atualizarContadorGrafico(modo, todosItens);
}

function atualizarContadorGrafico(modo, todosItens) {
  if (!graficoFiltrosAtivos) return;
  const total = todosItens?.length || 0;
  const ativos = graficoFiltrosAtivos.size;
  const isFiltrado = ativos < total && ativos > 0;
  const countId = modo === 'tipo' ? 'graficoCountTipo' : 'graficoCountStatus';
  const countEl = document.getElementById(countId);
  if (countEl) {
    countEl.style.display = isFiltrado ? 'inline' : 'none';
    countEl.textContent = String(total - ativos) + ' ocultos';
  }
}

// Fechar dropdowns do gráfico ao clicar fora
document.addEventListener('click', e => {
  if (!e.target || !document.contains(e.target)) return;
  if (!e.target.closest('#graficoDropdownTipoWrap')) {
    const el = document.getElementById('graficoDropdownTipo');
    const chev = document.getElementById('graficoChevronTipo');
    if (el) el.style.display = 'none';
    if (chev) chev.style.transform = 'rotate(0deg)';
  }
  if (!e.target.closest('#graficoDropdownStatusWrap')) {
    const el = document.getElementById('graficoDropdownStatus');
    const chev = document.getElementById('graficoChevronStatus');
    if (el) el.style.display = 'none';
    if (chev) chev.style.transform = 'rotate(0deg)';
  }
});

// Remover o bloco #graficoFiltros do HTML (checkboxes inline embaixo do gráfico)
// — substituído pelos dropdowns acima
```

### 5c. Remover bloco de checkboxes inline do gráfico no HTML

```html
<!-- REMOVER completamente do HTML: -->
<div id="graficoFiltros" style="margin-top:16px;padding-top:14px;border-top:...">
  ...
  <div id="graficoCheckboxes" style="display:flex;flex-wrap:wrap;gap:6px"></div>
</div>
```

---

## 6. `dashboard.html` — Memória de aba: resetar ao sair da página

### Substituir `localStorage` por `sessionStorage` para a memória de aba:

```js
// Em switchTab():
// DE:
localStorage.setItem('svn_dashboard_tab', tab);
// PARA:
sessionStorage.setItem('svn_dashboard_tab', tab);

// Em init(), na inicialização:
// DE:
let currentTab = localStorage.getItem('svn_dashboard_tab') || 'geral';
// PARA:
let currentTab = sessionStorage.getItem('svn_dashboard_tab') || 'geral';
```

---

## 7. `solicitacoes.html` — Opacidade dos botões inativos

### Reduzir de 0.6 para 0.5:

```js
// Em renderCategorias() ou onde os botões de tipo inativo são renderizados,
// localizar o style que define opacity nos itens desabilitados:

// Procurar por:
'.selection-btn.disabled { opacity: 0.45; }'
// ← já está em style.css como 0.45 ✅ — não precisa mudar

// O que precisa mudar são os botões das categorias inativas no menu.
// Localizar no script de solicitacoes.html onde ativo:false recebe estilo:

// DE (exemplo):
style="opacity:0.6"
// OU class="selection-btn disabled" (já tem opacity:0.45 no CSS)

// Se os botões inativos usam inline style opacity:0.6, substituir por 0.5:
// Procurar por: opacity:0.6 ou opacity:.6 ou 'disabled'
// nos botões da grade de seleção e ajustar para 0.5
```

**ATENÇÃO:** Para aplicar corretamente, localizar em `solicitacoes.html` 
onde a classe `disabled` é aplicada ou onde `ativo: false` gera o HTML,
e confirmar se o valor atual é 0.45 (CSS) ou 0.6 (inline). Se o CSS já
usa 0.45 e você quer 0.5, alterar `.selection-btn.disabled { opacity: 0.45 }` 
para `opacity: 0.5` em `style.css`.

---

## 8. `dashboard.html` — Filtro de Tipos por categoria + contador em todos os filtros

### 8a. Atualizar dropdown de Tipo para agrupar por categoria

```js
// Substituir o conteúdo estático do #dropdownTipo por geração dinâmica
// baseada em CATEGORIAS_SOLICITACAO do config.js:

function renderDropdownTipos() {
  const container = document.getElementById('dropdownTipo');
  if (!container) return;

  // Preservar o botão "Limpar" no final
  container.innerHTML = '<div style="font-size:0.72rem;font-weight:700;opacity:0.4;text-transform:uppercase;letter-spacing:.05em;padding:4px 6px 6px">Tipos</div>';

  CATEGORIAS_SOLICITACAO.forEach(cat => {
    // Filtrar apenas itens ativos E com FORM_ROUTES definido
    const itensAtivos = cat.itens.filter(item =>
      item.ativo &&
      typeof FORM_ROUTES !== 'undefined' &&
      (FORM_ROUTES[item.id] || FORM_ROUTES[item.id + '-dados'])
    );
    if (itensAtivos.length === 0) return;

    // Header da categoria
    const catHeader = document.createElement('div');
    catHeader.style.cssText = 'font-size:0.68rem;font-weight:700;opacity:0.35;text-transform:uppercase;letter-spacing:0.06em;padding:8px 8px 4px;margin-top:4px;border-top:1px solid var(--border-light)';
    catHeader.textContent = cat.categoria;
    container.appendChild(catHeader);

    itensAtivos.forEach(item => {
      // Mapear subtipos: pagina-assessores tem dados e atualizacao
      const tiposDoItem = item.id === 'pagina-assessores'
        ? ['pagina-assessores-dados', 'pagina-assessores-atualizacao']
        : item.id === 'apresentacao'
          ? ['apresentacao-nova', 'apresentacao-atualizar']
          : item.id === 'conteudo-pdf'
            ? ['conteudo-pdf-informativo', 'conteudo-pdf-ebook']
            : [item.id];

      tiposDoItem.forEach(tipoId => {
        const tipoLabel = (typeof TIPO_SOLICITACAO_LABELS !== 'undefined' && TIPO_SOLICITACAO_LABELS[tipoId]) || item.label;
        const label = document.createElement('label');
        label.className = 'dropdown-checkbox-label';
        label.innerHTML = `<input type="checkbox" value="${esc(tipoId)}" onchange="updateAdminTiposFiltro()">${esc(tipoLabel)}`;
        container.appendChild(label);
      });
    });
  });

  // Botão limpar
  const clearBtn = document.createElement('button');
  clearBtn.onclick = limparAdminTipos;
  clearBtn.style.cssText = 'margin-top:6px;width:100%;text-align:center;background:none;border:none;font-size:0.78rem;opacity:0.5;cursor:pointer;font-family:"Nunito Sans",sans-serif';
  clearBtn.textContent = 'Limpar';
  container.appendChild(clearBtn);
}
```

### 8b. Chamar `renderDropdownTipos()` ao inicializar o admin

```js
// Em initAdmin(), adicionar:
function initAdmin() {
  if (!Auth.isAdmin()) return;
  document.getElementById('tabAdmin').style.display = '';
  renderDropdownTipos(); // ← adicionar aqui
  document.addEventListener('click', e => { ... }); // listener existente
}
```

### 8c. Contador nos botões Tipo e Status da tabela admin

```js
// Atualizar updateAdminTiposFiltro():
function updateAdminTiposFiltro() {
  adminTiposSelecionados = new Set(
    [...document.querySelectorAll('#dropdownTipo input:checked')].map(c => c.value)
  );
  const btn = document.getElementById('adminFiltroTipoBtn');
  const count = adminTiposSelecionados.size;
  btn.style.borderColor = count ? 'var(--ruby-red)' : '';
  btn.style.color       = count ? 'var(--ruby-red)' : '';

  // Atualizar badge contador
  let badge = btn.querySelector('.filter-count-badge');
  if (!badge) {
    badge = document.createElement('span');
    badge.className = 'filter-count-badge';
    btn.appendChild(badge);
  }
  badge.style.display = count ? 'inline' : 'none';
  badge.textContent   = String(count);

  adminPageHistorico = 1;
  loadAdminHistorico();
}

// Atualizar updateAdminStatusFiltro():
function updateAdminStatusFiltro() {
  adminStatusesSelecionados = new Set(
    [...document.querySelectorAll('#dropdownStatus input:checked')].map(c => c.value)
  );
  const btn = document.getElementById('adminFiltroStatusBtn');
  const count = adminStatusesSelecionados.size;
  btn.style.borderColor = count ? 'var(--ruby-red)' : '';
  btn.style.color       = count ? 'var(--ruby-red)' : '';

  let badge = btn.querySelector('.filter-count-badge');
  if (!badge) {
    badge = document.createElement('span');
    badge.className = 'filter-count-badge';
    btn.appendChild(badge);
  }
  badge.style.display = count ? 'inline' : 'none';
  badge.textContent   = String(count);

  adminPageHistorico = 1;
  loadAdminHistorico();
}
```

### 8d. Remover o HTML estático do #dropdownTipo (substituído por JS dinâmico)

```html
<!-- REMOVER os <label> estáticos dentro do #dropdownTipo — serão gerados pelo renderDropdownTipos(): -->
<label class="dropdown-checkbox-label"><input type="checkbox" value="eventos" ...>Eventos</label>
<label class="dropdown-checkbox-label"><input type="checkbox" value="artes-divulgacao" ...>Arte de Divulgação</label>
<!-- ... todos os outros labels ... -->
<!-- Manter apenas a div wrapper vazia: -->
<div id="dropdownTipo" style="display:none;position:absolute;..."></div>
```

---

## OBSERVAÇÕES

- `style.css`: se `.selection-btn.disabled { opacity: 0.45 }` → mudar para `0.5`
- `dashboard.html`: único arquivo com todas as mudanças 1-6 e 8
- `solicitacao.html`: mudança 3 (badge responsável)
- `solicitacoes.html`: mudança 7 (opacidade botões inativos) — confirmar valor atual antes
- Nenhum build necessário
- A troca `localStorage` → `sessionStorage` na aba (item 6) preserva a memória
  durante navegação e refresh, mas reseta quando o usuário fecha a aba ou abre
  o dashboard em uma nova sessão do browser
