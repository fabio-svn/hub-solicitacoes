Diagnosticar por que as solicitações da **Lista Geral** não estão criando task no ClickUp, mesmo com o `listId` correto `901300673533`.

## Contexto confirmado

* Eventos continuam funcionando
* Lista Geral correta: `901300673533`
* Exemplo que falha: `artes-divulgacao`

## Objetivo

Fazer uma investigação técnica precisa e corrigir o ponto exato da falha.

---

# Ações obrigatórias

## 1. Adicionar logs completos no fluxo de criação da task da Lista Geral

Registrar obrigatoriamente:

* tipo recebido
* subtipo recebido
* listId escolhido
* nome final da task
* status enviado para criação
* payload exato enviado no `POST /list/{listId}/task`
* response status HTTP
* response body completo em caso de erro

## 2. Separar claramente as etapas

Logar cada uma destas fases:

### Fase A

Entrada na função de criação da task

### Fase B

Escolha do `listId`

### Fase C

Montagem do nome da task

### Fase D

Montagem do payload de criação

### Fase E

Resposta da API ClickUp

### Fase F

Tentativa de preenchimento dos custom fields

## 3. Garantir que o create da task aconteça ANTES dos custom fields

Se hoje o fluxo estiver tentando preencher campo ou validar coisa demais antes da criação, simplificar para:

1. criar task mínima
2. obter taskId
3. preencher custom fields depois

## 4. Fazer teste mínimo com task mínima

Para `artes-divulgacao`, testar primeiro criação com payload mínimo:

* `name`
* `description`
* `status` (somente se for válido)

Se falhar, testar sem `status` explícito e deixar o ClickUp usar o padrão da lista.

## 5. Validar status da Lista Geral

Verificar se o status usado atualmente realmente existe na Lista Geral.

### Se estiver sendo enviado algo como:

* "Para fazer"
* "To do"
* qualquer outro valor não existente

corrigir.

### Regra:

Se houver dúvida, testar criação sem status explícito.

## 6. Se a task mínima funcionar

A falha está nos custom fields ou na descrição nova.

Nesse caso:

* criar task mínima primeiro
* depois adicionar os custom fields um por um, com logs
* identificar qual campo quebra

## 7. Não falhar silenciosamente

Nunca retornar `null` sem log completo.

---

# Estratégia de diagnóstico obrigatória

Implementar uma validação em duas fases para `artes-divulgacao`:

## Teste 1

Criar task mínima na Lista Geral com:

* nome simples
* descrição simples
* sem custom fields
* sem payload complexo

## Teste 2

Se funcionar, adicionar:

* novo título humanizado
* nova descrição humanizada
* custom fields gradualmente

Objetivo:
isolar com precisão a etapa que quebra.

---

# Importante

Não alterar eventos.

Focar apenas na Lista Geral.

---

# Entrega esperada

Ao finalizar, informar com clareza:

1. se a task mínima foi criada ou não
2. qual payload exato foi enviado no teste mínimo
3. qual resposta exata a API do ClickUp devolveu
4. se o problema era:

   * status inválido
   * listId
   * tipo não reconhecido
   * custom field
   * descrição/payload
5. qual correção foi aplicada
6. arquivos alterados
