# Correção — Forms, Config e Admin

Aplicar todos os ajustes abaixo. Organizados por arquivo.

---

## 1. `config.js` — Corrigir IDs acentuados

### 1a. Corrigir IDs em `ITENS_MATERIAIS`
```js
// Substituir as entradas com acento:
{ id: "pacote-padrao",       label: "Pacote de Divulgação Padrão (convite + página de inscrição)", icon: "icon-envelope-star" },
{ id: "captacao-audiovisual", label: "Captação Audiovisual", icon: "icon-video" },
```

### 1b. Corrigir IDs em `ITENS_MATERIAIS_ONLINE`
```js
{ id: "pacote-padrao-online",       label: "Pacote de Divulgação Padrão - 2 dias úteis", icon: "icon-envelope-star" },
{ id: "pacote-personalizado-online", label: "Pacote de Divulgação Personalizado - 7 dias", icon: "icon-palette" },
```

### 1c. Corrigir chaves acentuadas em `PRAZOS_MATERIAIS`
```js
// Substituir:
"pacote-padrao":        { label: "15 dias de antecedência" },
"captacao-audiovisual": { label: "15 dias de antecedência" },
"pacote-padrao-online": { label: "2 dias úteis" },
```

### 1d. Corrigir chaves acentuadas em `DRAWER_FIELD_LABELS`
```js
// Substituir as chaves acentuadas por versões sem acento:
descricao:    "Descrição",
publico:      "Público",
conteudo:     "Conteúdo",
// Remover as entradas: "descrição", "público", "conteúdo"
```

### 1e. Corrigir IDs acentuados em `CAMPOS_ETAPA2_FORM2` e `CAMPOS_ETAPA2_FORM2_ONLINE`
```js
// Em CAMPOS_ETAPA2_FORM2, substituir:
{ id: "descricao",       label: "Descrição do evento" },
{ id: "publico",         label: "Público do evento" },

// Em CAMPOS_ETAPA2_FORM2_ONLINE, substituir:
{ id: "descricao",       label: "Descrição do evento" },
{ id: "publico",         label: "Público do evento" },
{ id: "link-transmissao", label: "Link da transmissão" },
```

### 1f. Corrigir parâmetro URL em `FORM_ROUTES`
```js
"pagina-assessores-atualizacao": "form-pagina-assessores.html?subtipo=atualizacao",
```

### 1g. Adicionar `cor` como alias de compatibilidade em `STATUS_SOLICITACAO`
Para não quebrar código legado que ainda use `statusObj.cor`:
```js
const STATUS_SOLICITACAO = [
  { id: "recebido",               label: "Recebido",                   bg: "#e8f4ea", text: "#2d7a3a",  cor: "--sage-green"    },
  { id: "alinhamentos",           label: "Alinhamentos",               bg: "#e8f0fb", text: "#1a56a0",  cor: "--sage-green"    },
  { id: "em-analise",             label: "Em análise",                 bg: "#fff3e0", text: "#b45309",  cor: "--ruby-red"      },
  { id: "em-andamento",           label: "Em andamento",               bg: "#fff3e0", text: "#b45309",  cor: "--ruby-red"      },
  { id: "em-producao",            label: "Em produção",                bg: "#fff3e0", text: "#b45309",  cor: "--ruby-red"      },
  { id: "cotacao-aprovacao",      label: "Em cotação / aprovação",     bg: "#fef3c7", text: "#92660a",  cor: "--leather-brown" },
  { id: "aguardando",             label: "Aguardando informação",      bg: "#fef3c7", text: "#92660a",  cor: "--leather-brown" },
  { id: "aguardando-rh",          label: "Aguardando aprovação do RH", bg: "#fce8f3", text: "#9d174d",  cor: "--leather-brown" },
  { id: "aguardando-pagamento",   label: "Aguardando pagamento",       bg: "#fce8f3", text: "#9d174d",  cor: "--leather-brown" },
  { id: "aguardando-finalizacao", label: "Aguardando finalização",     bg: "#ede9fe", text: "#5b21b6",  cor: "--leather-brown" },
  { id: "concluido",              label: "Concluído",                  bg: "#d1fae5", text: "#065f46",  cor: "--sage-green"    },
  { id: "reprovado",              label: "Reprovado / Cancelado",      bg: "#fee2e2", text: "#991b1b",  cor: "--ruby-red"      },
  { id: "cancelado",              label: "Cancelado",                  bg: "#fee2e2", text: "#991b1b",  cor: "--carbon-black"  },
  { id: "em-espera",              label: "Em espera",                  bg: "#f1f5f9", text: "#475569",  cor: "--carbon-black"  },
];
```

### 1h. Preencher `URL_APRESENTACAO`
```js
let URL_APRESENTACAO = "https://hub.portalsvn.com.br/solicitacoes.html";
// (ou o URL correto do form de apresentações — ajustar conforme necessário)
```

---

## 2. `form-eventos.html` — Corrigir IDs acentuados e outros bugs

### 2a. Remover importação de `transitions.js`
```html
<!-- REMOVER: -->
<script src="transitions.js"></script>
```

### 2b. Corrigir IDs acentuados em todos os `buildXFields()`

Em `buildPresencial1Fields()`, `buildPresencial2Fields()`, `buildPresencial3Fields()`, `buildOnline1Fields()`, `buildOnline2Fields()`, `buildOnline3Fields()` e `buildCampoField()`:

Substituir todas as ocorrências de:
- `id="descrição"` → `id="descricao"`
- `id="público"` → `id="publico"`

### 2c. Corrigir `saveFormState()` e `restoreFormState()`

Substituir a lista de fields por versão sem acentos:
```js
const fields = ['nomeEvento','dataEvento','horario','descricao',
  'origem','tipoEvento','publico','convidados','canal','linkTransmissao',
  'objetivos','custoEstimado','rateio','observacoes','localNome','localEndereco',
  'localSugestoes','ideaQuando'];
```

### 2d. Corrigir `submitForm()` — leitura dos campos

Substituir as linhas com IDs errados:
```js
descricao:    document.getElementById('descricao')?.value  || '',
publico:      document.getElementById('publico')?.value    || '',
```
(garantir que as chaves no objeto `dados{}` também ficam sem acento)

### 2e. Corrigir cases em `getMaterialConditional()`

Substituir todos os cases acentuados:
```js
case 'pacote-padrao': case 'pacote-padrao-online':
case 'captacao-audiovisual':
```

### 2f. Corrigir `updateMateriaisPreview()` — ICONS object

Substituir as chaves acentuadas no objeto `ICONS`:
```js
'pacote-padrao':         `...svg...`,
'captacao-audiovisual':  `...svg...`,
'pacote-padrao-online':  `...svg...`,
```

### 2g. Corrigir acentuação em textos visíveis

- `buildOnline3Fields()`: `'Reuniao aberta'` → `'Reunião aberta'`, `'Painel de discussao'` → `'Painel de discussão'`
- `showPalFields()`: `value="nao"` radio — o texto exibido `<span>Nao</span>` → `<span>Não</span>`
- `handleLocal()`: `'nao-definido'` value pode manter sem acento (é um identificador), mas o texto exibido `"Local ainda nao definido"` → `"Local ainda não definido"`
- `alert-text` na etapa de materiais: `"Se os materiais de apoio nao estiverem definidos, marque a opção Pacote Padrão, pois e utilizado na grande maioria dos eventos."` → `"Se os materiais de apoio não estiverem definidos, marque a opção Pacote Padrão, pois é utilizado na grande maioria dos eventos."`
- Modal de orientação: `"Quando as informações mais basicas do evento estiverem pre-definidas."` → `"Quando as informações mais básicas do evento estiverem pré-definidas."`, `"assistencia"` → `"assistência"`

### 2h. Corrigir `validateStep5()` — validar custo obrigatório

Substituir a função por:
```js
function validateStep5() {
  const custo = document.getElementById('custoEstimado')?.value.trim();
  if (!custo || custo === 'R$ 0,00') {
    const field = document.querySelector('#step5 .field');
    if (field) {
      field.classList.add('field-invalid');
      setTimeout(() => field.classList.remove('field-invalid'), 600);
    }
    return false;
  }
  const rateioField = document.getElementById('rateioField');
  if (rateioField && rateioField.style.display !== 'none') {
    const rateioVal = document.getElementById('rateio').value;
    if (!rateioVal || rateioVal === OPCOES_RATEIO[0]) {
      const field = document.querySelector('#rateioField .field');
      if (field) { field.classList.add('field-invalid'); setTimeout(() => field.classList.remove('field-invalid'), 600); }
      return false;
    }
  }
  return true;
}
```

---

## 3. `form-pagina-assessores.html` — Corrigir bugs

### 3a. Remover `transitions.js`
```html
<!-- REMOVER: -->
<script src="transitions.js"></script>
```

### 3b. Corrigir detecção de subtipo
```js
// Substituir:
const isAtualizacao = subtipo === 'atualização';
// Por:
const isAtualizacao = subtipo === 'atualização' || subtipo === 'atualizacao';
```

### 3c. Corrigir `saveDraft()` — remover campos inexistentes
```js
function saveDraft() {
  try {
    sessionStorage.setItem('form-assessores', JSON.stringify({
      nomeCompleto:   document.getElementById('nomeCompleto')?.value  || '',
      codigoAssessor: document.getElementById('codigoAssessor')?.value || '',
      unidade:        document.getElementById('unidade')?.value        || '',
      contratoSocial: document.getElementById('contratoSocial')?.value || '',
      linkedin:       document.getElementById('linkedin')?.value       || '',
      instagram:      document.getElementById('instagram')?.value      || '',
      miniBio:        document.getElementById('miniBio')?.value        || '',
    }));
  } catch {}
}
```

### 3d. Substituir `alert()` da foto por erro inline

```js
// Substituir:
if (!isAtualizacao && !document.getElementById('fotoPerfil').files[0]) {
  alert('Envie a foto de perfil.'); return;
}

// Por:
if (!isAtualizacao && !document.getElementById('fotoPerfil').files[0]) {
  const fieldEl = document.getElementById('fotoPerfil').closest('.field');
  if (fieldEl) {
    fieldEl.classList.add('field-invalid');
    let errorEl = fieldEl.querySelector('.field-error');
    if (!errorEl) {
      errorEl = document.createElement('div');
      errorEl.className = 'field-error';
      fieldEl.appendChild(errorEl);
    }
    errorEl.textContent = 'A foto de perfil é obrigatória.';
    fieldEl.scrollIntoView({ behavior: 'smooth', block: 'center' });
  }
  btn.disabled = false; btn.textContent = 'Enviar';
  return;
}
```

---

## 4. `form-apresentacoes.html` — Corrigir bugs

### 4a. Remover `transitions.js`
```html
<!-- REMOVER: -->
<script src="transitions.js"></script>
```

### 4b. Corrigir chave acentuada no objeto `dados{}`
```js
// Substituir:
público: público.join(', '),
// Por:
publicoAlvo: [...document.querySelectorAll('#publico input:checked')].map(c => c.value).join(', '),
```

### 4c. Corrigir acentuação no checkbox de elementos
```html
<!-- Substituir: -->
<label class="checkbox-option"><input type="checkbox" value="Graficos">...<span>Gráficos</span></label>
```

---

## 5. `form-criacao-pdf.html` — Corrigir bugs

### 5a. Remover `transitions.js`
```html
<!-- REMOVER: -->
<script src="transitions.js"></script>
```

### 5b. Corrigir chaves acentuadas no objeto `dados{}`
```js
// Substituir:
conteudo:    document.getElementById('conteudo').value,
publicoAlvo: [...document.querySelectorAll('#publico input:checked')].map(c => c.value).join(', '),
// Remover: conteúdo e público acentuados
```

---

## 6. `admin.html` — Corrigir bugs

### 6a. Remover `transitions.js`
```html
<!-- REMOVER: -->
<script src="transitions.js"></script>
```

### 6b. Adicionar `await _configReady` no `init()`
```js
async function init() {
  await _configReady; // adicionar como primeira linha
  await Auth.init();
  // ... resto igual
}
```

### 6c. Atualizar drawer do admin com `DRAWER_FIELD_LABELS` e `humanizeValue()`

No `openDrawer()` do `admin.html`, substituir o bloco que renderiza os campos por:

```js
body += '<div class="drawer-section"><div class="drawer-section-title">Dados</div>';
for (const [key, value] of Object.entries(dados)) {
  if (value === null || value === undefined || value === '') continue;
  const label = (typeof DRAWER_FIELD_LABELS !== 'undefined' && DRAWER_FIELD_LABELS[key])
    ? DRAWER_FIELD_LABELS[key] : key;

  if (Array.isArray(value)) {
    if (value.length === 0) continue;
    body += `<div class="drawer-field">
      <div class="drawer-field-label">${esc(label)}</div>
      <div class="drawer-field-value">${value.map(v => esc(String(v))).join(' · ')}</div>
    </div>`;
    continue;
  }
  if (typeof value === 'object') continue;

  const displayValue = (typeof humanizeValue === 'function') ? humanizeValue(key, value) : value;
  body += `<div class="drawer-field">
    <div class="drawer-field-label">${esc(label)}</div>
    <div class="drawer-field-value">${esc(String(displayValue))}</div>
  </div>`;
}
body += '</div>';
```

Adicionar também a função `humanizeValue()` e `getStatusColor()` no `<script>` do `admin.html` — copiar as mesmas funções do `dashboard.html`.

### 6d. Corrigir acentuação nos textos visíveis
```html
<!-- stat labels: -->
<div class="stat-label">Concluídas</div>
<div class="stat-label">Este mês</div>

<!-- título da seção de usuários: -->
<h2 style="font-size:1.1rem;font-weight:600">Gestão de usuários</h2>

<!-- filtro "Concluido": -->
<button class="filter-chip" data-value="concluido">Concluído</button>
```

### 6e. Tornar o drawer do admin não-bloqueante (igual ao dashboard)

Substituir `openDrawer()` para abrir imediatamente e buscar status em background:

```js
async function openDrawer(item) {
  // Abrir imediatamente
  document.getElementById('drawerOverlay').classList.add('visible');
  document.getElementById('drawer').classList.add('open');
  renderAdminDrawer(item);

  // Atualizar status em background
  try {
    const syncRes = await fetch('/api/solicitacoes/' + item.id + '/status');
    if (syncRes.ok) {
      const syncData = await syncRes.json();
      if (syncData.updated && syncData.status !== item.status) {
        item.status = syncData.status;
        renderAdminDrawer(item);
      }
    }
  } catch {}
}
```

Extrair a lógica de renderização para `renderAdminDrawer(item)` (mesmo padrão do `renderDrawerContent` no dashboard).

---

## OBSERVAÇÕES FINAIS

- Após corrigir os IDs em `form-eventos.html`, verificar se `buildCampoField()` também usa `id="descrição"` e `id="público"` nos cases — corrigir para `id="descricao"` e `id="publico"` nessa função também
- O `transitions.js` pode ser mantido como arquivo vazio ou deletado — as transições estão no `auth.js`
- As correções de IDs no `config.js` (ponto 1) devem ser aplicadas ANTES dos forms, pois os forms dependem dos IDs do config para renderizar os materiais
- Não alterar a ordem dos campos que foi definida manualmente — apenas corrigir os bugs listados

---

## 7. `form-eventos.html` — Ordenar campos do objeto `dados` no submitForm()

No `submitForm()`, substituir o objeto `dados` por versão com campos na ordem que devem aparecer no ClickUp:

```js
const dados = {
  // Identificação
  nome:           Auth.getUserName(),
  natureza:       formState.natureza,
  maturidade:     formState.maturidade === 1 ? 'Tenho a maioria das informações'
                : formState.maturidade === 2 ? 'Tenho algumas informações'
                : formState.maturidade === 3 ? 'Ainda estou estruturando' : '',
  // Evento
  nomeEvento:     document.getElementById('nomeEvento')?.value     || '',
  dataEvento:     document.getElementById('dataEvento')?.value     || '',
  horario:        document.getElementById('horario')?.value        || '',
  descricao:      document.getElementById('descricao')?.value      || '',
  origem:         document.getElementById('origem')?.value         || '',
  tipoEvento:     document.getElementById('tipoEvento')?.value     || '',
  publico:        document.getElementById('publico')?.value        || '',
  // Local
  estado:         document.getElementById('estado')?.value         || '',
  cidade:         document.getElementById('cidade')?.value         || '',
  localEvento:    document.getElementById('localEvento')?.value    || '',
  unidadeSVN:     document.getElementById('unidadeSVN')?.value     || '',
  localNome:      document.getElementById('localNome')?.value      || '',
  localEndereco:  document.getElementById('localEndereco')?.value  || '',
  // Logística
  convidados:     document.getElementById('convidados')?.value     || '',
  custoEstimado:  document.getElementById('custoEstimado')?.value  || '',
  rateio:         document.getElementById('rateio')?.value         || '',
  // Online
  canal:          document.getElementById('canal')?.value          || '',
  linkTransmissao:document.getElementById('linkTransmissao')?.value|| '',
  objetivos:      document.getElementById('objetivos')?.value      || '',
  // Materiais e tipo aproximado
  materiais:      [...formState.materiais],
  tipoAprox:      document.querySelector('input[name="tipoAprox"]:checked')?.value || '',
  ideaQuando:     document.getElementById('ideaQuando')?.value     || '',
  // Observações
  observacoes:    document.getElementById('observacoes')?.value    || '',
};

// Remover campos vazios para não poluir a descrição no ClickUp
const dadosLimpos = Object.fromEntries(
  Object.entries(dados).filter(([_, v]) => {
    if (Array.isArray(v)) return v.length > 0;
    return v !== '' && v !== null && v !== undefined;
  })
);
```

E em seguida, usar `dadosLimpos` no lugar de `dados` ao montar o `formData`:
```js
formData.append('dados', JSON.stringify(dadosLimpos));
```

Isso garante que:
1. Os campos aparecem no ClickUp na ordem lógica (identificação → evento → local → logística → materiais)
2. Campos vazios não aparecem na descrição da task, deixando o ClickUp mais limpo
3. Maturidade aparece como texto legível em vez de número (1/2/3)
