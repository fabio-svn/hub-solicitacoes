# Cleanup v3 — solicitacoes.html, thankyou.html, upload-feedback.js

---

## 1. `thankyou.html` — natureza 'patrocinio' não mapeada

```js
// Localizar o bloco que mapeia resumo.natureza:
// DE:
if (resumo.natureza) itens.push({ label: 'Natureza', valor: resumo.natureza === 'presencial' ? 'Presencial' : 'Online' });

// PARA:
if (resumo.natureza) {
  const naturezaMap = { presencial: 'Presencial', online: 'Online', patrocinio: 'Patrocínio' };
  itens.push({ label: 'Natureza', valor: naturezaMap[resumo.natureza] || resumo.natureza });
}
```

---

## 2. `thankyou.html` — sessionStorage.removeItem antes de exibir

```js
// Mover o removeItem para DEPOIS da animação de entrada do resumo:

// DE (dentro do if itens.length > 0):
setTimeout(() => {
  const card = document.getElementById('resumoCard');
  card.style.transition = 'opacity 0.4s ease';
  card.style.opacity = '1';
}, 1200);
sessionStorage.removeItem('svn_ultimo_resumo'); // ← antes da animação

// PARA:
setTimeout(() => {
  const card = document.getElementById('resumoCard');
  card.style.transition = 'opacity 0.4s ease';
  card.style.opacity = '1';
  sessionStorage.removeItem('svn_ultimo_resumo'); // ← após confirmar exibição
}, 1200);
```

---

## 3. `thankyou.html` — remover style.transform redundante

```js
// DE:
card.style.transition = 'all 0.5s ease';
card.style.opacity = '1';
card.style.transform = 'scale(1)'; // ← redundante

// PARA:
card.style.transition = 'opacity 0.5s ease';
card.style.opacity = '1';
// (remover a linha do transform)
```

---

## 4. `solicitacoes.html` — tooltip "Em breve" acessível em mobile

```js
// Botões inativos não mostram "Em breve" em mobile (sem hover).
// Adicionar onclick que mostra um feedback visual temporário:

// Substituir:
if (!item.ativo) btn.setAttribute('data-tooltip', 'Em breve');

// Por:
if (!item.ativo) {
  btn.setAttribute('data-tooltip', 'Em breve');
  btn.addEventListener('click', function() {
    // Feedback visual temporário para mobile
    const original = btn.style.outline;
    btn.style.outline = '2px solid rgba(34,27,25,0.2)';
    btn.style.transition = 'outline 0.3s';
    // Mostrar label "Em breve" inline por 1.5s
    const label = btn.querySelector('.btn-label');
    if (label) {
      const originalText = label.textContent;
      label.textContent = 'Em breve';
      label.style.opacity = '0.5';
      setTimeout(() => {
        label.textContent = originalText;
        label.style.opacity = '';
        btn.style.outline = original;
      }, 1500);
    }
  });
}
```

---

## 5. `upload-feedback.js` — listener duplo em campos dinâmicos

```js
// O problema: FileUpload.bind() adiciona um addEventListener no input.
// Se o mesmo input for recriado no DOM e bind() chamado novamente,
// o novo input é diferente então não há problema.
// MAS se o mesmo elemento DOM tiver bind() chamado duas vezes
// (ex: renderMateriais() chamada duas vezes), o listener dispara duas vezes.
//
// Fix: usar um data attribute para marcar inputs já vinculados:

bind(inputId, nameElId, options) {
  const input  = typeof inputId  === 'string' ? document.getElementById(inputId)  : inputId;
  const nameEl = typeof nameElId === 'string' ? document.getElementById(nameElId) : nameElId;
  if (!input) return;

  // Evitar listener duplo no mesmo elemento
  if (input.dataset.uploadBound === '1') return;
  input.dataset.uploadBound = '1';

  options = options || {};
  input.addEventListener('change', function(e) {
    const f = e.target.files[0];
    if (!f) { FileUpload.clear(nameEl); return; }
    if (options.accept) {
      const allowed = options.accept.split(',')
        .map(function(x) { return x.trim().toLowerCase().replace(/^\./,''); })
        .filter(Boolean);
      const ext = f.name.split('.').pop().toLowerCase();
      if (allowed.length && !allowed.includes(ext)) {
        FileUpload.error(nameEl, 'Formato não permitido. Use: ' + allowed.map(function(x){ return x.toUpperCase(); }).join(', '));
        input.value = '';
        return;
      }
    }
    if (options.maxMB && f.size > options.maxMB * 1024 * 1024) {
      FileUpload.error(nameEl, 'Arquivo excede o tamanho máximo de ' + options.maxMB + ' MB');
      input.value = '';
      return;
    }
    FileUpload.success(nameEl, f);
    if (options.onChange) options.onChange(f, nameEl, input);
  });
},
```

---

## OBSERVAÇÕES

- Nenhum dos três arquivos precisa de build
- O fix do upload-feedback.js é o mais importante — evita disparo duplo
  de validação em forms com campos dinâmicos (form-eventos.html)
- O fix do thankyou.html é cosmético mas garante que o resumo sempre
  aparece antes de ser removido do sessionStorage
- O tooltip mobile em solicitacoes.html é melhoria de UX, não bug crítico
