# Cleanup geral v2 — solicitacao.html, form-eventos.html, form-pagina-assessores.html, form-apresentacoes.html, form-criacao-pdf.html, form-artes-divulgacao.html, form-atualizacao-material.html, clickup.ts, schema confirmado

---

## SCHEMA — Confirmado, zero pendências

Todas as colunas já existem: `titulo`, `clickup_url`, `avaliacao`, `responsavel`.
Nenhuma migration necessária.

---

## 1. `solicitacao.html` — Bugs e melhorias

### 1a. BUG — Modal de avaliação não fecha ao clicar fora

```js
// A função fecharModalAvaliacao verifica e.target mas o modal usa display:flex
// então o clique fora chega no div externo. O problema é que onclick="fecharModalAvaliacao(event)"
// checa if (e && e.target !== ...) — mas quando chamada sem argumento (botão ×), e é undefined.
// Isso causa erro silencioso. Corrigir:

function fecharModalAvaliacao(e) {
  if (e && e.target !== document.getElementById('avaliacaoModal')) return;
  document.getElementById('avaliacaoModal').style.display = 'none';
}

// Substituir por:
function fecharModalAvaliacao(e) {
  // Sem argumento (botão ×) ou clique direto no overlay
  if (!e || e.target === document.getElementById('avaliacaoModal')) {
    document.getElementById('avaliacaoModal').style.display = 'none';
  }
}
```

### 1b. BUG — `syncStatus` pode sobrescrever renderPage com dados desatualizados

```js
// Problema: syncStatus chama renderPage(item) mas item é o objeto original passado
// por referência. Se renderPage for chamado duas vezes em paralelo (raro mas possível),
// pode haver race condition. Corrigir com flag:

let _pageRendered = false;

async function syncStatus(item) {
  try {
    const r = await fetch('/api/solicitacoes/' + solicitacaoId + '/status');
    if (!r.ok) return;
    const d = await r.json();
    if (d.updated && d.status !== item.status) {
      item.status = d.status;
      renderPage(item);
    }
  } catch {}
}
```

### 1c. MELHORIA — `renderDados` skip de chaves técnicas incompleto

```js
// SKIP_KEYS atual: { 'idSolicitacao', 'natureza', 'setor' }
// Adicionar mais chaves que são metadados internos e não devem aparecer nos dados:
const SKIP_KEYS = new Set([
  'idSolicitacao', 'natureza', 'setor', 'nome',
  'materiaisDetalhes', // objeto complexo, já processado em materiais
]);
```

### 1d. MELHORIA — Botão WhatsApp não tem `display:none` como fallback antes do JS

```html
<!-- O botão flutuante começa em bottom:78px pelo CSS mas o JS reposiciona.
     Em solicitacao.html há dois float-home, então o WhatsApp deve começar
     calculado. O fallback de 78px no CSS está correto pois esta página
     tem dois botões flutuantes — verificar se posicionarWhatsApp() calcula certo:
     maiorAltura = 78px (botão Menu em bottom:78px) → wa.bottom = 78 + 58 = 136px ✅
     
     Nenhuma mudança necessária, só confirmar que o botão Menu nesta página
     realmente tem bottom:78px inline e não usa o default do float-home (24px).
-->
```

### 1e. MELHORIA — `aprovacaoBody` max-height hardcoded em 700px

```js
// Ao abrir, body.style.maxHeight = '700px' é hardcoded.
// Se o chat crescer além de 700px (histórico de rodadas longo), vai ser cortado.
// Substituir por altura maior ou dinâmica:
body.style.maxHeight = '900px';
```

---

## 2. `form-eventos.html` — Bugs e melhorias

### 2a. BUG — `buildPresencial2Fields()` e `buildOnline2Fields()` usam `CAMPOS_ETAPA2_FORM2`

```js
// Essas constantes devem ser REMOVIDAS do config.js (conforme cleanup anterior),
// MAS os forms de eventos ainda as usam para o modo de maturidade 2.
// DECISÃO: manter CAMPOS_ETAPA2_FORM2 e CAMPOS_ETAPA2_FORM2_ONLINE no config.js
// pois ainda têm uso ativo nos forms de eventos. Reverter a remoção proposta.
// Atualizar o comentário no config.js:
// "Usados em form-eventos.html para o modo de maturidade 2 (informação parcial)"
```

### 2b. BUG — `validateStep2()` para maturidade 1 valida todos os inputs mas ignora campos hidden

```js
// O campo 'horBrasilia' é um radio sem 'required', então validateStep2 não o pega.
// Mas o campo 'dataEvento' TEM required e está dentro de buildPresencial1Fields().
// Verificar que o campo de cidade desabilitado é realmente ignorado:
// ✅ já tem: if (input.id === 'cidade' && input.disabled) return;

// BUG: validateStep2 para mat===1 pega TODOS os inputs, inclusive
// os campos de rateio dentro de #rateioField que pode estar display:none.
// Adicionar verificação de visibilidade:
function isFieldVisible(el) {
  let cur = el;
  while (cur && cur !== document.body) {
    const style = window.getComputedStyle(cur);
    if (style.display === 'none' || style.visibility === 'hidden') return false;
    cur = cur.parentElement;
  }
  return true;
}

// Usar em validateStep2():
container.querySelectorAll('input:not([type="file"]), select, textarea').forEach(input => {
  if (input.id === 'cidade' && input.disabled) return;
  if (!isFieldVisible(input)) return; // ← adicionar esta linha
  checkField(input);
});
```

### 2c. BUG — `restoreCondMaterialFields()` chamado antes de `renderMateriais()`

```js
// Em activateStep(), para step === 3:
if (step === 3) { renderMateriais(); restoreCondMaterialFields(); bindCondUploadHandlers(); }
// renderMateriais() cria os elementos dos campos condicionais,
// mas não abre os blocos dos materiais selecionados antes do restore.
// Os campos condicionais só aparecem se o material estiver selecionado.
// Corrigir: forçar abertura dos campos antes de restaurar valores:

if (step === 3) {
  renderMateriais();
  // Abrir campos condicionais dos materiais já selecionados
  formState.materiais.forEach(id => {
    const condBlock = document.getElementById('cond-' + id);
    if (condBlock) condBlock.classList.add('open');
  });
  restoreCondMaterialFields();
  bindCondUploadHandlers();
}
```

### 2d. MELHORIA — `saveFormState()` não salva o estado dos palestrantes

```js
// O estado dos palestrantes (nome, cargo, SVN) não é salvo no sessionStorage.
// Se o usuário recarregar na step 4, perde os dados. Adicionar ao saveFormState():

// Adicionar ao objeto data:
data._temPalestrante = document.querySelector('input[name="temPalestrante"]:checked')?.value || '';
data._qtdPalestrante = document.querySelector('input[name="qtdPalestrante"]:checked')?.value || '';
for (let i = 1; i <= 4; i++) {
  data['_palSvn' + i] = document.querySelector(`input[name="palSvn${i}"]:checked`)?.value || '';
  data['_palNome' + i] = document.getElementById('palNome' + i)?.value || '';
  data['_palCargo' + i] = document.getElementById('palCargo' + i)?.value || '';
}
```

### 2e. MELHORIA — `submitForm()` no form de eventos não mostra erro quando backend retorna 4xx

```js
// Atual:
else {
  const errEl = document.getElementById('submitError');
  if (errEl) { errEl.style.display = 'block'; errEl.textContent = 'Erro ao enviar. Tente novamente.'; }
  btn.disabled = false; btn.textContent = 'Enviar';
}

// Melhorar para mostrar mensagem específica quando disponível:
else {
  let errMsg = 'Erro ao enviar. Tente novamente.';
  try {
    const errData = await res.json();
    if (errData.error) errMsg = errData.error;
  } catch {}
  const errEl = document.getElementById('submitError');
  if (errEl) { errEl.style.display = 'block'; errEl.textContent = errMsg; }
  btn.disabled = false; btn.textContent = 'Enviar';
}
```

### 2f. INCONSISTÊNCIA — Botão Next step 4 usa lógica inline para determinar próximo step

```js
// Atual (frágil e difícil de manter):
onclick="validateStep4() && goStep(formState.natureza==='presencial'&&formState.maturidade!==3?5:6)"

// Substituir por função nomeada:
function getNextStepFrom4() {
  return formState.natureza === 'presencial' && formState.maturidade !== 3 ? 5 : 6;
}
// E no HTML:
onclick="validateStep4() && goStep(getNextStepFrom4())"

// Mesmo padrão para o botão Voltar do step 6:
onclick="goStep(formState.natureza==='presencial'&&formState.maturidade!==3?5:4)"
// → substituir por:
function getPrevStepFrom6() {
  return formState.natureza === 'presencial' && formState.maturidade !== 3 ? 5 : 4;
}
onclick="goStep(getPrevStepFrom6())"
```

---

## 3. `form-pagina-assessores.html` — Bugs e melhorias

### 3a. BUG — `saveDraft()` e `restoreDraft()` rodam antes do `init()`

```js
// No final do script:
document.querySelectorAll('input,textarea,select').forEach(el => el.addEventListener('change', saveDraft));
restoreDraft();
// Isso roda imediatamente, mas os campos ainda não foram preenchidos com as
// opções do init() (unidades, contratos, selos). restoreDraft() pode restaurar
// valores que os selects ainda não têm como opções, fazendo o value voltar a "".
// Mover restoreDraft() para dentro do init(), após popular os selects:

async function init() {
  // ...
  // Após popular os selects:
  UNIDADES_SVN.forEach(...);
  CONTRATOS_SOCIAIS.forEach(...);
  renderSelos();
  SETORES.forEach(...);
  FileUpload.bind(...);
  
  restoreDraft(); // ← mover para cá
  if (subtipo) goToStep2(); // ← manter depois do restoreDraft
}
```

### 3b. BUG — `openPreview()` não verifica se o elemento existe antes de usar

```js
// Linha:
const instagram = document.getElementById('instagram') ? document.getElementById('instagram').value : '';
// Simplificar e estender padrão para todos os campos:
const instagram = document.getElementById('instagram')?.value || '';
const linkedin = document.getElementById('linkedin')?.value || '';
// (já está assim para linkedin, inconsistência visual apenas)
```

### 3c. MELHORIA — `submitForm()` usa `alert()` para erro

```js
// Substituir os dois alert() por exibição no formulário:
// DE:
else { alert('Erro ao enviar.'); btn.disabled = false; btn.textContent = 'Enviar'; }
catch (e) { alert('Erro de conexão.'); btn.disabled = false; btn.textContent = 'Enviar'; }

// PARA: adicionar um div de erro antes do form-nav e exibir lá:
// No HTML, adicionar antes do form-nav:
// <div id="submitError" class="alert-card alert-danger" style="display:none;margin-bottom:12px"></div>

// No JS:
function showSubmitError(msg) {
  const el = document.getElementById('submitError');
  if (el) { el.style.display = 'block'; el.textContent = msg; el.scrollIntoView({ behavior: 'smooth', block: 'center' }); }
}
// Substituir os alert() por showSubmitError(...)
```

### 3d. MELHORIA — Validação de foto obrigatória feita após disable do botão

```js
// Atual: btn.disabled = true → btn.textContent = 'Enviando...' → valida foto
// Se a foto não existe, o botão fica desabilitado enquanto reabilita.
// Mover a validação da foto para ANTES de desabilitar o botão:

async function submitForm() {
  if (isAtualizacao && camposSelecionados.size === 0) { ... return; }
  if (!validateRequiredFields()) return;

  // Validar foto ANTES de desabilitar o botão:
  if (!isAtualizacao && !document.getElementById('fotoPerfil').files[0]) {
    // ... mostrar erro ...
    return; // ← early return limpo
  }

  const btn = document.getElementById('btnSubmit');
  btn.disabled = true; btn.textContent = 'Enviando...';
  // ... resto do submit
}
```

---

## 4. `form-apresentacoes.html` — Bugs e melhorias

### 4a. BUG — Arquivo base duplicado no FormData

```js
// Atual:
const base = document.getElementById('arquivoBase').files[0];
if (base) formData.append('arquivoBase', base);
const baseNova = document.getElementById('arquivoBaseNova')?.files[0];
if (baseNova) formData.append('arquivoBase', baseNova); // ← mesmo campo name 'arquivoBase'!

// Quando isAtualizar, arquivoBase vem de #arquivoBase (campo único do form de atualização).
// Quando isNova com base-existente, vem de #arquivoBaseNova.
// Os dois nunca existem simultaneamente, mas o campo name é o mesmo.
// No backend, arquivosMap['arquivoBase'] funcionará para ambos.
// ✅ Na prática funciona, mas o código é confuso. Adicionar comentário:
// Ambos usam 'arquivoBase' como campo no FormData — o backend trata iguais via arquivosMap.
```

### 4b. MELHORIA — `restoreDraft()` não restaura setor por causa da inicialização async

```js
// Mesmo problema que form-pagina-assessores: mover restoreDraft() para dentro do init()
// após popular o select de setores:

async function init() {
  // ...
  const setorSel = document.getElementById('setor');
  if (setorSel) { SETORES.filter(...).forEach(...); }
  
  restoreDraft(); // ← após popular setores
}
// Remover restoreDraft() standalone no final do script
```

### 4c. MELHORIA — `validateStep2()` tem função `isVisible` definida localmente

```js
// A função isVisible em validateStep2() é idêntica à que deve existir em form-eventos.html.
// Para evitar duplicação, poderia ser uma função global, mas como cada form é independente
// isso é aceitável. Adicionar apenas comentário:
// Nota: função isVisible é duplicada intencionalmente — cada HTML é independente.
```

---

## 5. `form-criacao-pdf.html` — Bugs e melhorias

### 5a. MELHORIA — `restoreDraft()` mesmos problemas de ordem de inicialização

```js
// Mover restoreDraft() para dentro de init() após popular setores:
async function init() {
  // ...
  const setorSel = document.getElementById('setor');
  if (setorSel) { SETORES.filter(...).forEach(...); }
  
  restoreDraft(); // ← após popular setores
}
```

### 5b. MELHORIA — Step 2 não tem botão Voltar

```html
<!-- O form-criacao-pdf.html no step2 não tem botão Voltar — apenas Próximo.
     O usuário não consegue voltar ao step1 para mudar o tipo (informativo/ebook)
     sem recarregar a página. Adicionar: -->
<div class="form-nav" style="justify-content:space-between">
  <button class="btn btn-secondary" onclick="goStep(1)">Voltar</button>
  <button class="btn btn-primary" onclick="validateStep2() && goStep(3)">Próximo</button>
</div>
```

---

## 6. `form-artes-divulgacao.html` — Bugs e melhorias

### 6a. MELHORIA — `restoreSession()` não restaura setor

```js
// restoreSession() tenta restaurar setor com:
if (data.setor) { const el = document.getElementById('setor'); if (el) el.value = data.setor; }
// Mas o select de setor é populado no init() após restoreSession() ser chamado.
// Mover restoreSession() para o final do init() ou usar setTimeout:
async function init() {
  // ...
  const setorSel = document.getElementById('setor');
  if (setorSel) { SETORES.filter(...).forEach(...); }
  
  restoreSession(); // ← mover para cá, após popular setores
}
```

### 6b. INCONSISTÊNCIA — `validateRequiredFields()` genérico vs `validateStep1()` específico

```js
// O form tem ambos: validateStep1() para o step 1 e validateRequiredFields() para o step 2.
// validateStep1() verifica canais e campos de texto manualmente.
// validateRequiredFields() verifica input[required] genérico.
// Isso é OK mas validateStep1() tem lógica mais robusta.
// SUGESTÃO: usar validateStep1() no step 1 (já está sendo usado) e garantir
// que todos os campos obrigatórios do step 2 têm required no HTML — eles têm ✅.
// Nenhuma mudança necessária, só documentar.
```

---

## 7. `form-atualizacao-material.html` — Limpo

Nenhum bug crítico encontrado. Apenas uma melhoria menor:

### 7a. MELHORIA — Não tem botão Voltar (adequado para form de 1 step)

```js
// Form de etapa única — correto não ter botão Voltar. ✅
```

---

## 8. `clickup.ts` — Bugs e melhorias

### 8a. BUG — `assigneeId` é string mas `parseInt()` pode retornar NaN

```ts
// Atual:
if (assigneeId) taskPayload.assignees = [parseInt(assigneeId)];

// Se CLICKUP_ASSIGNEE_GERAL ou EVENTOS não estiver configurado,
// assigneeId = "" e o `if (assigneeId)` protege.
// Mas se vier um valor não-numérico, parseInt retorna NaN e a API do ClickUp
// recebe [NaN] e ignora silenciosamente.
// Corrigir com validação explícita:
const assigneeIdNum = parseInt(assigneeId);
if (!isNaN(assigneeIdNum)) {
  taskPayload.assignees = [assigneeIdNum];
  logger.info({ assigneeId, assigneeIdNum, tipo }, "ClickUp: assignee definido");
} else if (assigneeId) {
  logger.warn({ assigneeId, tipo }, "ClickUp: assigneeId inválido (não numérico), pulando");
}
```

### 8b. INCONSISTÊNCIA — `buildDetailsSection()` para `atualizacao-material` usa `dados.descricao`

```ts
// Em buildDetailsSection para "atualizacao-material":
const descricao = str(dados.descricao);
if (descricao) items.push(`• Descrição das atualizações:\n${descricao}`);

// Mas em form-atualizacao-material.html o campo é enviado como dados.descricao ✅
// E em buildGeneralDescription, addLine(resumoItems, "Finalidade", str(dados.finalidade))
// também está correto para esse form ✅
// Nenhuma mudança necessária.
```

### 8c. MELHORIA — `UNIDADES_ENDERECOS` duplicado em clickup.ts e config.js

```ts
// O mapa UNIDADES_ENDERECOS dentro de setEventosCustomFields está duplicado
// do array UNIDADES_SVN do config.js (browser-side).
// Comentar a duplicidade intencional:
// NOTA: duplicado intencionalmente de UNIDADES_SVN em config.js (browser-side).
// Ao adicionar/alterar unidades, atualizar AMBOS os locais.
const UNIDADES_ENDERECOS: Record<string, string> = { ... };
```

### 8d. MELHORIA — `setEventosCustomFields` injeta `_cidadeFormatada` mutando o objeto `dados`

```ts
// Atual:
(dados as Record<string, unknown>)._cidadeFormatada = sigla ? `${cidadeRaw} - ${sigla}` : cidadeRaw;

// Isso muta o objeto dados passado por referência, que depois é usado
// para gerar a descrição e salvar no banco. A mutation é inofensiva
// porque _cidadeFormatada começa com _ (é ignorado por renderDados no frontend)
// e não é salvo diretamente (dados é JSONB). Mas é um efeito colateral implícito.
// SUGESTÃO: criar uma cópia local:

const dadosLocal = { ...dados } as Record<string, unknown>;
if (cidadeRaw) {
  const sigla = IBGE_SIGLA_MAP[estadoRaw] || estadoRaw;
  dadosLocal._cidadeFormatada = sigla ? `${cidadeRaw} - ${sigla}` : cidadeRaw;
}
// Usar dadosLocal apenas dentro de setEventosCustomFields,
// passando para setClickUpCustomField via EVENTOS_CUSTOM_FIELDS.
```

---

## RESUMO DE PRIORIDADES

**Bugs reais a corrigir:**
1. `form-eventos.html` — campos hidden validados erroneamente em maturidade 1
2. `form-eventos.html` — campos condicionais não abertos antes de restoreCondMaterialFields
3. `form-pagina-assessores.html` — restoreDraft antes dos selects serem populados
4. `form-artes-divulgacao.html` — mesmo problema de ordem de init
5. `form-criacao-pdf.html` — sem botão Voltar no step 2
6. `clickup.ts` — parseInt(assigneeId) sem validação de NaN
7. `solicitacao.html` — fecharModalAvaliacao chamada sem argumento causa comportamento errado

**Melhorias menores (baixo risco):**
- submitForm() com mensagem de erro específica do backend (todos os forms)
- saveFormState() incluindo estado dos palestrantes
- Validação de foto antes de desabilitar o botão (form-assessores)
- Lógica de próximo step extraída para funções nomeadas (form-eventos)
- aprovacaoBody max-height de 700px → 900px
