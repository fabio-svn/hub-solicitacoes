No arquivo public/dashboard.html, há um bug de markup HTML: o <div> com style flex que envolve a barra de filtros + barra de busca da tab-eventos NÃO foi fechado, fazendo com que tab-geral fique como FILHO de tab-eventos no DOM. Como tab-eventos tem display:none, a tab-geral inteira fica invisível e nenhuma solicitação aparece.

LOCALIZAR (este bloco específico, dentro do <div class="tab-content" id="tab-eventos">):

        <div class="search-bar" style="flex:1;margin-bottom:0;height:32px;padding:0 14px;border-radius:8px">
          <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" opacity="0.4"><circle cx="11" cy="11" r="8"/><line x1="21" y1="21" x2="16.65" y2="16.65"/></svg>
          <input type="text" placeholder="Buscar por nome do evento..." id="searchEventos" oninput="debounceSearch('eventos')">
        </div>
      <div id="listEventos"></div>
      <div class="pagination" id="paginationEventos"></div>
    </div>

SUBSTITUIR POR (adicionar `</div>` faltante após o fechamento do search-bar):

        <div class="search-bar" style="flex:1;margin-bottom:0;height:32px;padding:0 14px;border-radius:8px">
          <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" opacity="0.4"><circle cx="11" cy="11" r="8"/><line x1="21" y1="21" x2="16.65" y2="16.65"/></svg>
          <input type="text" placeholder="Buscar por nome do evento..." id="searchEventos" oninput="debounceSearch('eventos')">
        </div>
      </div>
      <div id="listEventos"></div>
      <div class="pagination" id="paginationEventos"></div>
    </div>

VERIFICAR DEPOIS:
1. Mesmo bug PODE existir no tab-geral. Procurar pela mesma estrutura no <div class="tab-content active" id="tab-geral"> (próximo do final do arquivo). Se tiver o mesmo padrão, aplicar mesmo fix.

2. Após aplicar, rodar no terminal:
   grep -c '<div\b' public/dashboard.html  
   grep -c '</div>' public/dashboard.html
   
   Os dois números devem ser IGUAIS (atualmente: 75 abre, 73 fecha — depois do fix: ambos iguais).

3. Recarregar /dashboard.html no navegador. As 20 solicitações da lista devem aparecer normalmente.